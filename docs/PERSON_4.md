# PERSON_4 — إضافة المتصفح والاختبار والتوثيق

**عدد المهام: ٤**

هذا الملف جزء من **خطة الأسبوع الفعلية**، وهي أربعة ملفات:
[`PERSON_1.md`](PERSON_1.md) · [`PERSON_2.md`](PERSON_2.md) ·
[`PERSON_3.md`](PERSON_3.md) · [`PERSON_4.md`](PERSON_4.md).
مجموع مهام الخطة **٢٠ مهمة**. أما [`PROJECT_TASKS.md`](PROJECT_TASKS.md) فهو
Backlog مستقبلي وليس خطة هذا الأسبوع.

**قائد الفريق هو PERSON_1.**

---

## قواعد إرشادية فقط

- التوزيع في هذه الملفات **اقتراح تنظيمي**، وليس ملكية.
- **أي عضو يستطيع تعديل أي ملف يحتاجه** لإنجاز مهمته.
- **قائد الفريق يستطيع نقل أي مهمة** بين الأعضاء في أي وقت.
- لا توجد ملكية إجبارية لأي مجلد، ولا موافقات، ولا قيود آلية.
- لا Issues ولا Branch Protection ولا CODEOWNERS ولا Git Hooks ولا CI Gates.
- **الاختبارات للتأكد والتسجيل فقط.** فشل اختبار **لا يمنع حفظ العمل**. سجّل
  الفشل كما ظهر، وأبلغ قائد الفريق، واستمر.
- Claude لا ينفّذ `commit` أو `push` أو `merge`، ولا ينشئ أو يحذف فروعًا.

## نقطة البداية الجاهزة

مجلد [`extension/`](../extension/) فيه **Starter يعمل الآن**: Manifest V3،
ونافذة عربية RTL، وحقل رسالة وزر إرسال، وعرض رد الـBackend، وحالتا Loading و
Error، وإعداد قابل للتغيير لـAPI Base URL، و`README` يشرح Load unpacked.

قيود ثابتة للإضافة:

- **HTML و CSS و JavaScript مباشر فقط.** لا React ولا أي Build System.
- **لا Content Scripts** ولا أي صلاحية لقراءة صفحات المستخدم.

---

# P4-01 — تطوير إضافة المتصفح

- **الحالة:** Done
- **تعتمد على:** الـStarter الموجود في `extension/`
- **الملفات المتوقعة:** `extension/`
- **النتيجة:** [أسفل المهمة](#نتيجة-p4-01)

## ما تشمله

1. **ربط الإضافة بالمصادقة:** تسجيل دخول من داخل النافذة، وحفظ الـToken في
   `chrome.storage.local`، وإرساله مع كل طلب، وتسجيل خروج.
2. **سجل المحادثة داخل النافذة:** عرض الرسائل السابقة بدل رد واحد فقط،
   وتمرير تلقائي إلى الأسفل، وزر «محادثة جديدة».
3. الاحتفاظ بالمحادثة الأخيرة بعد إغلاق النافذة وإعادة فتحها.
4. **تحسين حالات الخطأ:** جلسة منتهية، اشتراك منتهٍ، سيرفر متوقف، إذن نطاق
   غير ممنوح — لكل حالة رسالة عربية واضحة وإجراء مقترح.
5. صقل النافذة: أيقونة الإضافة، وارتفاع مناسب بلا قفزات، ودعم لوحة المفاتيح.

## شرط الإنجاز

- الإضافة تعمل بعد Load unpacked وترسل وتستقبل من الـBackend.
- **صفر Content Scripts وصفر صلاحيات قراءة صفحات** في `manifest.json`.
- تبديل رابط السيرفر ما زال يعمل.

```
اقرأ docs/PERSON_4.md و extension/README.md.
نفّذ المهمة P4-01. تأكد من أنك داخل C:\Users\abdul\govagent-repo عبر
git rev-parse --show-toplevel قبل أي تعديل.
افحص مجلد extension/ كاملًا قبل التعديل. طوّر الـStarter الموجود ولا تعِد
إنشاءه.
لا تعمل Commit أو Push أو Merge، ولا تنشئ فروعًا أو Issues.

قيود ثابتة: HTML و CSS و JavaScript مباشر فقط. لا React ولا Build System.
لا Content Scripts ولا أي صلاحية لقراءة صفحات المستخدم.

المطلوب:
1. أضف تسجيل دخول من داخل النافذة: احفظ الـToken في chrome.storage.local،
   وأرسله مع كل طلب، وأضف تسجيل خروج.
2. اعرض سجل المحادثة داخل النافذة بدل رد واحد فقط، مع تمرير تلقائي إلى
   الأسفل وزر «محادثة جديدة».
3. احتفظ بالمحادثة الأخيرة بعد إغلاق النافذة وإعادة فتحها.
4. حسّن رسائل الخطأ: جلسة منتهية، اشتراك منتهٍ، سيرفر متوقف، إذن نطاق غير
   ممنوح — لكل حالة رسالة عربية واضحة وإجراء مقترح.
5. اصقل النافذة: أيقونة للإضافة، ارتفاع مستقر بلا قفزات، ودعم لوحة المفاتيح.

جرّب فعليًا عبر Load unpacked مع تشغيل الـBackend، واذكر ما رأيته.
في النهاية اذكر الملفات المعدّلة ونتيجة التجربة كما ظهرت فعلًا.
```

## نتيجة P4-01

**الحالة: Done.** نُفِّذت بتطوير الـStarter الموجود، بلا إعادة إنشاء وبلا
React وبلا Build System. ملفات `lib/` وحدات ES يحمّلها المتصفح مباشرة.

### الملفات

| الملف                     | الحالة  |
| ------------------------- | ------- |
| `extension/lib/api.js`    | مضاف    |
| `extension/lib/storage.js`| مضاف    |
| `extension/lib/errors.js` | مضاف    |
| `extension/icons/*.png`   | مضاف (16 · 32 · 48 · 128) |
| `extension/manifest.json` | معدّل   |
| `extension/popup.html`    | معدّل   |
| `extension/popup.css`     | معدّل   |
| `extension/popup.js`      | معدّل   |
| `extension/README.md`     | معدّل   |

### صلاحيات `manifest.json` — كما قرأها المتصفح بعد التحميل

```
permissions         : ["storage"]
host_permissions    : ["http://localhost:8000/*", "http://127.0.0.1:8000/*"]
optional_host_perms : ["http://*/*", "https://*/*"]
content_scripts     : غير موجود  →  صفر Content Scripts
tabs / activeTab / scripting / <all_urls> : غير موجودة
```

قُرئت من `chrome.runtime.getManifest()` داخل الإضافة المحمّلة، لا من الملف.
و`chrome.permissions.getAll()` أعادت النطاقين المحليين و`storage` فقط.

### ما جُرّب فعلًا

الإضافة **حُمِّلت Load unpacked** في Chrome 151 بملف تعريف نظيف، والـBackend
يعمل على `localhost:8000` بـ`DATA_STORE=memory` و`MODEL_PROVIDER=mock`.
كل بند أدناه نُفِّذ في النافذة نفسها ونتيجته مقروءة من الـDOM:

| # | الحالة | النتيجة |
| - | ------ | ------- |
| 1 | فتح النافذة بلا جلسة | شاشة الدخول |
| 2 | كلمة مرور خاطئة | «البريد الإلكتروني أو كلمة المرور غير صحيحة.» |
| 3 | بريد بصيغة خاطئة | تحقق محلي قبل إرسال الطلب |
| 4 | دخول صحيح | شاشة المحادثة، والشريط السفلي يعرض الاسم والجهة |
| 5 | حفظ الرمز | `accessToken` و `tokenExpiresAt` و `user` في `chrome.storage.local` |
| 6 | إرسال بـ`Enter` | فقاعة الموظف ثم رد الإيجنت، و`conversation_id` محفوظ |
| 7 | `Shift+Enter` | سطر جديد بلا إرسال |
| 8 | أربع رسائل متتالية | ٨ فقاعات، والسجل ممرَّر إلى الأسفل تلقائيًا |
| 9 | إعادة فتح النافذة | استُعيدت المحادثة نفسها (٤ رسائل) من السيرفر |
| 10 | «محادثة جديدة» | السجل فارغ، والمعرّف مُسح، والرسالة التالية فتحت محادثة جديدة |
| 11 | محادثة محذوفة من السيرفر | «لم تعد المحادثة السابقة موجودة» والمعرّف نُسي |
| 12 | السيرفر متوقف | «تعذّر الاتصال بـ…» + زر «افتح الإعدادات»، والنص عاد إلى الحقل |
| 13 | جلسة منتهية عند الفتح | «انتهت جلستك…» والرمز مُسح |
| 14 | جلسة تسقط أثناء الاستخدام | عاد إلى شاشة الدخول، والرمز والمحادثة مُسحا |
| 15 | اشتراك منتهٍ | «انتهى اشتراك جهتك بتاريخ 2025-06-30…» **مع بقائه داخل الجلسة** |
| 16 | إذن نطاق غير ممنوح | «لم تمنح المتصفحَ إذنَ الاتصال…» + زر «امنح الإذن» |
| 17 | تسجيل الخروج | شاشة الدخول، والرمز والمحادثة مُسحا |
| 18 | `Escape` | يغلق الإعدادات ويعيد التركيز إلى زرّها |
| 19 | تغيير رابط السيرفر | حُفظ، وأُنهيت الجلسة بشرح السبب، والدخول والمحادثة عملا على الرابط الجديد |
| 20 | ارتفاع النافذة | ثابت عند 560px في كل الحالات أعلاه، والسجل وحده يتمرّر |

**الجلسة الساقطة والاشتراك المنتهي عولجا مختلفَين عمدًا:** 401 تُخرج الموظف
إلى شاشة الدخول، أما 403 فتُبقيه داخلًا وتعرض رسالة الـBackend بتاريخها —
إخراجه منها يوهم أن المشكلة في بيانات دخوله.

### ما لم يُجرَّب

- **Edge.** الاختبار كله جرى على Chrome 151 وحده.
- **الضغط على أيقونة الإضافة في شريط الأدوات.** فُتحت `popup.html` على أصل
  الإضافة نفسه (`chrome-extension://<id>/popup.html`)، وهو الكود والصلاحيات
  والـCORS نفسها، لكن ليس إطار النافذة المنبثقة الذي يرسمه المتصفح.
- **نافذة إذن النطاق الحقيقية.** جُرّبت حالتا «ممنوح» و«غير ممنوح»، ولم
  يُضغط «Allow» في نافذة المتصفح يدويًا.
- **مزود مودل حقيقي.** كل الردود من `mock` — `oracle` ضمن P1-01.
- **`sources`** (مصادر ملفات الجهة): الكود يعرضها، ولم تُرفع ملفات لتظهر.

### الاختبارات

`cd backend && .venv/Scripts/python -m pytest` → **391 passed · 4 skipped**
(المتخطّاة تحتاج Oracle) · 0 failed. لم يُعدَّل أي ملف في `backend/`.

> **ملاحظة لقائد الفريق:** `README.md` في الجذر ما زال يصف الإضافة كنسخة
> ترسل رسالة وتعرض ردًّا، بلا ذكر تسجيل الدخول. لم يُعدَّل لأنه ملف مشترك،
> وتحديثه ضمن **P4-04**.

---

# P4-02 — البيانات التجريبية ومجموعة Postman

- **الحالة:** Done
- **تعتمد على:** لا شيء (تتحسّن بعد عمل PERSON_2)
- **الملفات المتوقعة:** `mock-data/` · `docs/`
- **النتيجة:** [أسفل المهمة](#نتيجة-p4-02)

## ما تشمله

1. **بيانات جهات ومستخدمين تجريبية:** جهتان على الأقل، ولكل جهة مسؤول وعدة
   موظفين، بأدوار واشتراكات مختلفة (واحد ساري وواحد منتهٍ).
2. **بيانات محادثات وملفات تجريبية:** محادثات بأطوال مختلفة، ورسائل عربية
   واقعية، وملفات بأسماء وأنواع مختلفة.
3. **لا أسرار حقيقية إطلاقًا:** لا كلمات مرور حقيقية ولا بيانات حكومية
   حقيقية. كلمات المرور التجريبية مذكورة كنص واضح **في التوثيق فقط**، لا في
   قاعدة البيانات.
4. **مجموعة Postman** تغطي كل المسارات: صحة الخدمة، الدخول، المحادثات،
   الرسائل، رفع الملفات، لوحة المسؤول.
5. متغيرات بيئة في المجموعة (`base_url` و `token`) بحيث يعمل الجميع بلا تعديل
   يدوي، مع خطوة دخول تملأ الـToken تلقائيًا.

## شرط الإنجاز

- عضو جديد يستورد المجموعة ويشغّلها من أول طلب إلى آخره.
- البيانات التجريبية تغطي حالة الاشتراك المنتهي وحالة جهتين مختلفتين.

```
اقرأ docs/PERSON_4.md.
نفّذ المهمة P4-02. تأكد من أنك داخل C:\Users\abdul\govagent-repo عبر
git rev-parse --show-toplevel قبل أي تعديل.
لا تعمل Commit أو Push أو Merge، ولا تنشئ فروعًا أو Issues.

المطلوب:
1. وسّع mock-data/: جهتان على الأقل، ولكل جهة مسؤول وعدة موظفين، بأدوار
   واشتراكات مختلفة — واحد ساري وواحد منتهٍ.
2. أضف محادثات وملفات تجريبية: أطوال مختلفة، ورسائل عربية واقعية، وأسماء
   وأنواع ملفات مختلفة.
3. ممنوع أي سر حقيقي أو بيانات حكومية حقيقية. كلمات المرور التجريبية تُذكر
   في التوثيق فقط، ولا تُخزَّن كنص صريح في أي مكان آخر.
4. أنشئ مجموعة Postman تغطي: /health، تسجيل الدخول، المحادثات، الرسائل، رفع
   الملفات، ومسارات لوحة المسؤول.
5. استخدم متغيرات base_url و token في المجموعة، واجعل طلب الدخول يملأ الـToken
   تلقائيًا حتى تعمل بقية الطلبات بلا تعديل يدوي.

في النهاية اذكر الملفات المضافة والمعدّلة، وما جرّبته فعليًا.
```

## نتيجة P4-02

**الحالة: Done.**

### الملفات

| الملف                                             | الحالة |
| ------------------------------------------------- | ------ |
| `mock-data/conversations.json`                    | مضاف   |
| `mock-data/files.json`                            | مضاف   |
| `mock-data/files/لائحة-الإجازات.txt`              | مضاف   |
| `mock-data/files/تعميم-اعتماد-المخططات.txt`       | مضاف   |
| `mock-data/files/دليل-الخدمات-الإلكترونية.docx`   | مضاف   |
| `mock-data/files/نموذج-طلب-إجازة.pdf`             | مضاف   |
| `mock-data/files/جدول-الميزانية.xlsx`             | مضاف   |
| `docs/postman/GovAgent.postman_collection.json`   | مضاف   |
| `docs/postman/GovAgent.postman_environment.json`  | مضاف   |
| `docs/postman/README.md`                          | مضاف   |
| `mock-data/README.md`                             | معدّل  |

`mock-data/organizations.json` **لم يُعدَّل**: قورن بمخزن الـBackend صفًا صفًا
(٣ جهات و٨ مستخدمين وحالات الاشتراك الثلاث) فتطابق تمامًا بلا انحراف.

### البيانات التجريبية

- **المحادثات:** سبع محادثات بأطوال مقصودة الاختلاف — ثلاث برسالتين،
  واثنتان بأربع، وواحدة بعشر، و**واحدة فارغة بلا رسائل**. إحداها في الجهة
  المنتهي اشتراكها.
  فيها محادثة موظف ومحادثة مسؤول جهته معًا، لإثبات أن المحادثة خاصة
  بصاحبها لا بجهته.
- **الملفات:** خمسة ملفات بأربع صيغ — `.txt` و `.docx` و `.pdf` وواحد
  `.xlsx` غير مدعوم عمدًا، وواحد يخص الجهة الثانية لاختبار العزل.
- **بلا أسرار:** لا كلمة مرور في أي ملف بيانات، والمحتوى مُختلَق بالكامل
  على نطاقات `.test`.

### مجموعة Postman

**٩ مجلدات و٤٢ طلبًا**، تغطي المسارات المطلوبة كلها: `/health` والدخول
والمحادثات والرسائل والمحادثة والملفات والجهة والموظفين والاشتراك وإعدادات
المودل وسجل التدقيق وتجهيز جهة جديدة وحالات الخطأ.

- `{{base_url}}` و `{{token}}`، وتسجيل الدخول يحفظ الرمز تلقائيًا مع
  `{{organization_id}}` و `{{user_id}}`. وطلب ثانٍ يحفظ `{{admin_token}}`
  لمسارات المسؤول.
- **لا كلمة مرور مخزّنة في المجموعة ولا البيئة**: `dev_password` و
  `new_user_password` و `provisioning_key` متغيّرات `secret` **فارغة**.
  القيمة التجريبية مذكورة في `docs/postman/README.md` وحده. القيمة الصريحة
  الوحيدة هي `definitely-wrong` في اختبار «كلمة مرور خاطئة»، وهي كلمة
  **غير صالحة عمدًا** لا سرًّا.
- بُنيت بسكربت يتحقق آليًا من أمرين ويفشل عند أيهما: **أن كل مسار موجود في
  OpenAPI**، و**أن كل رمز حالة تؤكده الاختبارات معلن في OpenAPI**.

### رفع الملف بمسار نسبي — تصحيح بعد المراجعة

كانت النسخة الأولى تترك حقل الملف فارغًا فتتطلب اختيارًا يدويًا، فلا تحقق
شرط «عضو جديد يشغّلها من أول طلب إلى آخره». صار الطلب يقرأ ملفًا **من داخل
المستودع بمسار نسبي**:

```
mock-data/files/لائحة-الإجازات.txt
```

نسبي لا مطلق، فلا يخص جهازًا ولا نظام تشغيل، ولا ملف خارج المستودع. وأُضيف
طلب **«سؤال يستشهد بالملف المرفوع»** بعد الرفع، يؤكد أن `sources` تحمل
`لائحة-الإجازات.txt` — أي أن الملف فُهرِس فعلًا وصار قابلًا للبحث. بذلك
صار العدد ٤٢ طلبًا بدل ٤١.

**مجلد التشغيل موثَّق** في `docs/postman/README.md §٥`: من جذر المستودع
لـnewman، أو `--working-dir` من أي مجلد آخر، أو **Settings ← General ←
Working directory** في Postman. والتشغيل من مجلد خاطئ يعطي خطأً صريحًا
يذكر الحل.

### ما جُرّب فعلًا

شُغّلت بـ**newman 6.2.2** (مُشغّل Postman الرسمي) على الـBackend الحيّ،
**بالأمر الموثّق حرفيًا ومن جذر المستودع، بلا حقن مسار ولا تعديل على
المجموعة**:

| الفحص | النتيجة |
| ----- | ------- |
| عدد الطلبات | **42 / 42** · صفر فشل |
| التأكيدات | **97 / 97** · صفر فشل |
| رفع ملف مدعوم | **201** وحالته `processed` |
| ظهور الملف في `sources` | ✅ `لائحة-الإجازات.txt` مع `file_id` و `score` |
| أسرار في المجموعة أو البيئة | **لا شيء** — كل حقول كلمات المرور متغيّرات |
| مسار مطلق | **لا شيء** |

وأُعيد التشغيل على **سيرفر أُقلع من جديد** (مخزن ذاكرة فارغ) فأعطى النتيجة
نفسها — أي أن المجموعة لا تعتمد على حالة سابقة.

وتحقّقت من أن الشرط حقيقي: التشغيل من `docs/postman/` يفشل برسالة
`file load error: "mock-data/files/لائحة-الإجازات.txt", no such file`،
ويُصلحه `--working-dir ../..`.

رفع الملفات الأربعة على الـBackend مباشرة: الثلاثة المدعومة **201
`processed`**، والـ`.xlsx` **415** برسالة تشرح البديل. واستُخرج نصّ الـPDF
بـ`pypdf` بنجاح.

**أربعة أخطاء في المجموعة اكتُشفت أثناء التشغيل وأُصلحت:**

1. `DELETE /api/files/{id}` يعيد **200** مع سجل الملف، لا 204 كما افترضتُ.
2. `POST /api/organizations` بمفتاح خاطئ يعيد **401**، لا 403.
3. `PATCH .../subscription` بمفتاح خاطئ يعيد **401**، لا 403.
4. وصف المجموعة بقي يقول «اختر الملف يدويًا» بعد إصلاح المسار — صار
   يشرح شرط مجلد التشغيل.

الثاني والثالث اكتشفهما حارس رموز الحالة آليًا. و`DELETE /api/users/{id}`
صار يؤكد **200 مع `is_active = false`** — تعطيل لا حذف.

### ما لم يُجرَّب

- **واجهة Postman الرسومية.** الاستيراد والتشغيل تمّا بـnewman، وهو
  المُشغّل نفسه وصيغة المجموعة نفسها (Collection v2.1)، لكن لم تُفتح
  نافذة Postman ولم يُجرَّب زر Import ولا ضبط Working Directory من
  الإعدادات يدويًا.
- **حالتا 413 (حجم زائد) و«اسم بلا امتداد».** لا ملف لهما في المستودع.
- **`DATA_STORE=oracle`.** كل التشغيل على مخزن الذاكرة.

> **ملاحظة لقائد الفريق:** تشغيل مجلدَي «تجهيز جهة جديدة» و«تعديل
> الاشتراك» يحتاج `PROVISIONING_KEY` مضبوطًا على السيرفر؛ بدونه يعيدان
> **503** وهو سلوك مقصود لا عطل. واختبارات المجموعة تقبل الحالتين.

---

# P4-03 — حالات الاختبار الآلية

- **الحالة:** Done
- **تعتمد على:** عمل PERSON_1 و PERSON_2
- **الملفات المتوقعة:** `backend/tests/` · `docs/`
- **النتيجة:** [أسفل المهمة](#نتيجة-p4-03)

## ما تشمله

اختبارات `pytest` تغطي الحالات الحرجة:

1. **صحة الخدمة والمحادثة:** `/health` يعيد `ok`، و`/api/chat` يعمل بـ`mock`،
   والرسالة الفارغة تُرفض بـ422.
2. **المصادقة:** دخول صحيح، كلمة مرور خاطئة، مستخدم غير موجود، Token تالف،
   Token منتهٍ، ومسار محمي بلا Token.
3. **انتهاء الاشتراك:** جهة باشتراك منتهٍ تُمنع برسالة واضحة، وجهة سارية
   تعمل طبيعيًا.
4. **عزل الجهات:** مستخدم جهة (أ) لا يقرأ ولا يعدّل ولا يحذف أي محادثة أو
   ملف أو مستخدم من جهة (ب). **هذا أهم اختبار في الملف.**
5. **رفع الملفات:** امتداد مدعوم ينجح، وغير مدعوم يُرفض، والحجم الزائد
   يُرفض، والملف يُنسب للجهة الصحيحة.

**مهم:** الاختبارات **للتأكد والتسجيل فقط**، ولا تمنع حفظ العمل. سجّل الفشل
كما ظهر وأبلغ قائد الفريق. لا تعطّل اختبارًا لتمرير النتيجة.

## شرط الإنجاز

- كل الحالات أعلاه مكتوبة، وكل نتيجة مسجّلة كما ظهرت.
- الاختبارات التي تحتاج Oracle تُتخطّى بوضوح (`skip`) بدل أن تنهار.

```
اقرأ docs/PERSON_4.md.
نفّذ المهمة P4-03. تأكد من أنك داخل C:\Users\abdul\govagent-repo عبر
git rev-parse --show-toplevel قبل أي تعديل.
لا تعمل Commit أو Push أو Merge، ولا تنشئ فروعًا أو Issues.

المطلوب: اكتب اختبارات pytest في backend/tests/ تغطي:
1. /health يعيد ok، و/api/chat يعمل بـmock، والرسالة الفارغة تُرفض بـ422.
2. المصادقة: دخول صحيح، كلمة مرور خاطئة، مستخدم غير موجود، Token تالف،
   Token منتهٍ، ومسار محمي بلا Token.
3. انتهاء الاشتراك: جهة باشتراك منتهٍ تُمنع برسالة واضحة، وجهة سارية تعمل.
4. عزل الجهات: مستخدم جهة أ لا يقرأ ولا يعدّل ولا يحذف أي محادثة أو ملف أو
   مستخدم من جهة ب. غطِّ كل مسار قراءة وكتابة.
5. رفع الملفات: امتداد مدعوم ينجح، وغير مدعوم يُرفض، والحجم الزائد يُرفض،
   والملف يُنسب للجهة الصحيحة.

الاختبارات التي تحتاج Oracle حقيقية: علّمها skip بسبب واضح بدل أن تنهار.
الاختبارات للتأكد والتسجيل فقط. لا تعطّل أي اختبار فاشل ولا تخفِ أي فشل.

شغّل: cd backend && .venv/Scripts/python -m pytest
في النهاية اذكر الملفات المضافة ونتائج الاختبارات كاملة كما ظهرت فعلًا،
بما فيها الفاشلة والمتخطّاة.
```

## نتيجة P4-03

**الحالة: Done.**

### أولًا: إنهاء الوضع الانتقالي في `/api/chat`

كان المسار يقبل الطلبات بلا رمز دخول ليبقى قابلًا للتجربة قبل أن تضيف
الواجهة والإضافة تسجيل الدخول. أُنجز ذلك في **P3-01** و **P4-01**، فأُغلق:

| الملف | التغيير |
| ----- | ------- |
| `backend/app/api/chat.py` | `OptionalCurrentUser` ← **`CurrentUser`**، وحُذف فرع «بلا رمز» |
| `backend/app/schemas/chat.py` | حُذف حقل **`history`** و`MAX_HISTORY_MESSAGES` |
| `backend/app/services/chat_service.py` | تصحيح توثيق `history` — صار سياق السيرفر لا العميل |
| `backend/app/main.py` | وصف مجموعة `chat` في Swagger كان يعلن الوضع الانتقالي |

* **الجهة من الرمز وحدها**: `organization_id=user.organization_id` دائمًا.
* **الطلب بلا رمز يعيد 401** بالغلاف العربي الموحّد
  `{detail, status, code, errors}` وترويسة `WWW-Authenticate: Bearer`.
* الرفض **يسبق التحقق من الجسم**: رسالة فارغة بلا رمز تعيد 401 لا 422 —
  دليل أن الطلب لا يُعالَج أصلًا.

**حقل `history` صار مُتجاهَلًا لا مرفوضًا بـ422.** الحقل حُذف من الـschema،
وpydantic يتجاهل الحقول الزائدة — كما يفعل مع `organization_id` المحذوف في
P2-02، وله اختبار قائم يشترط التجاهل. إضافة `extra="forbid"` كانت ستناقض
ذلك القرار المُختبَر وتوسّع النطاق، فاكتُفي بإثبات **الأثر**: السياق
الملفّق لا يصل إلى المزود إطلاقًا.

### ثانيًا: تحقق العملاء — مُجرَّب حيًّا لا مقروءًا من الكود

| العميل | ترويسة `Authorization` | حقول الجسم | رمز الرد |
| ------ | ---------------------- | ---------- | -------- |
| الإضافة | `Bearer eyJhbGciOi…` | `["message"]` | **200** |
| الواجهة | `Bearer eyJhbGciOi…` | `["message"]` | **200** |

قُرئت الطلبات من شبكة المتصفح فعليًا (CDP `Network.requestWillBeSent`)، لا
من الكود: **لا `history` ولا `organization_id` في أي منهما**.

### ثالثًا: الاختبارات

**فُحصت التغطية القائمة أولًا، ولم تُكرَّر:** المصادقة (٤٠ اختبارًا في
`test_auth.py`) والاشتراك (`test_subscriptions.py`) والملفات
(`test_files.py`) والعزل (`test_isolation.py`) كانت تغطي المطلوب ٢ و٣ و٥
وأكثر المطلوب ٤. أُضيفت الحالات الناقصة وحدها.

| الملف | الحالة | ما فيه |
| ----- | ------ | ------ |
| `backend/tests/test_chat_protection.py` | **مضاف** | ١٣ اختبارًا — المطلوب ٦ كاملًا |
| `backend/tests/test_isolation.py` | معدّل | +٢ عزل عبر الجهات |
| `backend/tests/test_api.py` | معدّل | المطلوب ١ بعد الحماية |
| `backend/tests/test_conversations.py` | معدّل | اختبارات الوضع الانتقالي |
| `backend/tests/test_rag.py` | معدّل | اختبارا «بلا رمز» |

**المطلوب ٦ — `/api/chat` بلا رمز (`test_chat_protection.py`):** يعيد 401 ·
بالغلاف العربي الموحّد · بترويسة `WWW-Authenticate` · **لا يصل المزود**
(`send_message` و `get_model_provider` مُستبدَلان بدالة تنفجر إن استُدعيت) ·
**لا يجري أي بحث** (`retrieve_context` كذلك) · لا `sources` ولا `reply` ولا
`conversation_id` في الرد · لا تُحفظ محادثة · والرمز المنتهي والفارغ يُرفضان.

وحارسا انحدار يفشلان إن عاد أحد إلى الوضع الانتقالي: **لا حقل `history`
ولا `organization_id` في `ChatRequest`** في مواصفة OpenAPI، و**`/api/chat`
يعلن شرط مصادقة** (`security: [{"JWT": []}]`).

**المطلوب ٤ — الثغرات المضافة:** مستخدم جهة (ب) لا يقرأ رسائل محادثة جهة
(أ) ولا يعيد تسميتها ولا يحذفها (كلها 404، والمحادثة سليمة بعدها)، ولا
يُكمل محادثتها بـ`conversation_id` عبر `/api/chat` — ولا تُلحق بها رسالته.

### رابعًا: النتائج كما ظهرت

```
cd backend && .venv/Scripts/python -m pytest
403 passed, 4 skipped, 1 warning in 100.41s
```

**صفر فشل.** والمتخطّاة الأربع كلها Oracle بأسباب واضحة مكتوبة:

| الاختبار | سبب التخطّي |
| -------- | ----------- |
| `test_database.py:293` | Pending Oracle Setup — يحتاج قاعدة Oracle حقيقية |
| `test_document_ingestion.py:334` | Pending Oracle Setup — يحتاج عمود `VECTOR` |
| `test_oracle_provider.py:238` | Pending Oracle Setup — يحتاج بيانات OCI |
| `test_rag.py:600` | Pending Oracle Setup — يحتاج `VECTOR_DISTANCE` |

```
cd frontend && npm run build
✓ Compiled successfully in 2.9s  ·  Finished TypeScript  ·  7/7 static pages
```

```
npx eslint .
(بلا مخرجات — exit 0)
```

**تحذير واحد باقٍ ولم يُعالَج:** `StarletteDeprecationWarning` عن
`httpx`/`httpx2` من داخل `fastapi.testclient` — خارج كودنا، وترقية المكتبة
لا علاقة لها بهذه المهمة.

### خامسًا: أثر جانبي عولج

تغيير `/api/chat` أسقط تأكيدًا في مجموعة Postman (P4-02): طلب «422 —
history مع رمز دخول» صار يعيد 200. حُدِّث ليؤكد السلوك الجديد — أن السياق
الملفّق لا يظهر في الرد — وأُعيد تشغيل المجموعة كاملة:
**42/42 طلبًا · 99/99 تأكيدًا · صفر فشل**.

> **ملاحظة لقائد الفريق — يلزم قراره:** `README.md` في الجذر ما زال يصف
> الوضع الانتقالي في أربعة مواضع (السطور ٢٢٧ و٣٢١ و٣٢٤–٣٣٢)، ومنها فقرة
> تشرح «خطوات الإنهاء» التي نُفِّذت الآن. لم يُعدَّل لأنه ملف مشترك، وتحديثه
> ضمن **P4-04**. أما `backend/app/main.py` فعُدِّل سطر واحد فيه لأن وصفه
> يُنشَر في Swagger وكان يعلن سلوكًا لم يعد قائمًا.

---

# P4-04 — الفحص اليدوي والتوثيق النهائي

- **الحالة:** Done
- **تعتمد على:** عمل بقية الأعضاء
- **الملفات المتوقعة:** `docs/` · `README.md` · `extension/README.md`
- **النتيجة:** [أسفل المهمة](#نتيجة-p4-04)

## ما تشمله

1. **قائمة فحص يدوي للواجهة:** خطوات مرقّمة تغطي الدخول والمحادثة والرفع
   والإعدادات ولوحة المسؤول وتسجيل الخروج، بنتيجة متوقعة لكل خطوة.
2. **فحص عبر الأجهزة:** جوال ولوحي ومكتبي، على Chrome و Edge على الأقل،
   مع تسجيل ما نجح وما لم ينجح فعلًا.
3. **دليل التثبيت:** خطوات تشغيل النظام من الصفر لجهة جديدة — المتطلبات،
   الإعداد، تشغيل الـBackend والـFrontend، وتحميل الإضافة.
4. **سيناريو العرض (Demo):** نص عرض قصير خطوة بخطوة يُظهر أقوى ما في المشروع
   في وقت محدود، مع بيانات الدخول التجريبية.
5. **قالب تقرير الأخطاء** في `docs/`: الخطوات، والمتوقع، والفعلي، والبيئة،
   والأهمية.
6. تحديث `README.md` و `extension/README.md` بأي تغيير حصل خلال الأسبوع.

## شرط الإنجاز

- شخص خارج الفريق يستطيع تشغيل المشروع من الدليل وحده.
- قائمة الفحص اليدوي منفَّذة فعلًا ونتائجها مسجّلة، لا مكتوبة نظريًا.

```
اقرأ docs/PERSON_4.md و README.md و extension/README.md.
نفّذ المهمة P4-04. تأكد من أنك داخل C:\Users\abdul\govagent-repo عبر
git rev-parse --show-toplevel قبل أي تعديل.
لا تعمل Commit أو Push أو Merge، ولا تنشئ فروعًا أو Issues.

المطلوب:
1. اكتب في docs/ قائمة فحص يدوي للواجهة: خطوات مرقّمة تغطي الدخول والمحادثة
   ورفع الملفات والإعدادات ولوحة المسؤول وتسجيل الخروج، بنتيجة متوقعة لكل
   خطوة.
2. نفّذ فحصًا عبر الأجهزة (جوال ولوحي ومكتبي) على Chrome و Edge، وسجّل ما نجح
   وما لم ينجح فعلًا لا نظريًا.
3. اكتب دليل تثبيت يشرح تشغيل النظام من الصفر لجهة جديدة: المتطلبات،
   الإعداد، تشغيل الـBackend والـFrontend، وتحميل الإضافة عبر Load unpacked.
4. اكتب سيناريو عرض (Demo) قصيرًا خطوة بخطوة يُظهر أقوى ما في المشروع، مع
   بيانات الدخول التجريبية.
5. أضف قالب تقرير أخطاء في docs/: الخطوات، والمتوقع، والفعلي، والبيئة،
   والأهمية.
6. حدّث README.md و extension/README.md بأي تغيير حصل خلال الأسبوع.

في النهاية اذكر الملفات المعدّلة، ونتائج الفحص اليدوي كما ظهرت فعلًا.
```

## نتيجة P4-04

**الحالة: Done.** وهي آخر مهام `PERSON_4`.

### أولًا: تصحيح التوثيق بعد إغلاق الوضع الانتقالي

`README.md` كان يصف `/api/chat` بأنه يعمل بلا رمز، وفيه فقرة «خطوات
الإنهاء» التي نُفِّذت في P4-03. حُذف الوصف القديم وحلّ محلّه الوضع الصحيح:
**المسار يتطلب `Bearer Token` دائمًا، والجهة تأتي من الرمز، والسياق من
السيرفر، والطلب بلا رمز يعيد 401**.

### ثانيًا: صقل بصري — بلا تغيير في نظام الألوان ولا إعادة بناء

**الواجهة** — كل الوظائف باقية (الدخول، الشات، الرفع، لوحة المسؤول، PWA):

| المشكلة | ما تغيّر |
| ------- | -------- |
| مساحة المحادثة فارغة ومبالغ فيها | الحالة الفارغة تتوسّط رأسيًا، ومعها **أربع بطاقات اقتراح** تملأ الفراغ وتشرح ما يصلح له الإيجنت |
| الرسائل تمتد بعرض الشاشة | عمود واحد `max-w-3xl` يحاذي صندوق الكتابة أسفله |
| الهيدر والهوية غير متوازنة | مربّع هوية في ترويسة الشريط الجانبي، **وترويسة جديدة للوحة** تعرض عنوان المحادثة وحالتها |
| قائمة المحادثات ضعيفة | كل محادثة **بطاقة** بحدّ ومسافات، والنشطة بخلفية وشريط حافة، وعنوان قسم «المحادثات» |
| صندوق الكتابة مبعثر | الحقل وزرّا الإرفاق والإرسال داخل **إطار واحد** بحلقة تركيز موحّدة، وزر الإرسال أيقونة بحجم ثابت لا يقفز مع تغيّر النص |
| أزرار الإعدادات والخروج غير متناسقة | صارا **نصفين متساويين** في شبكة، وفوقهما بطاقة الموظف |

**الإضافة:**

| المشكلة | ما تغيّر |
| ------- | -------- |
| الالتصاق باليمين وفراغ أبيض في تبويب كامل | `margin: 0 auto` + `@media (min-width: 480px)` تجعلها **حاوية ٦٤٠px متوسّطة** تملأ الارتفاع. الاستعلام لا يتحقق في النافذة المنبثقة لأن عرضها = عرض الجسم |
| النص المختلط عربي/إنجليزي | `unicode-bidi: plaintext` على الفقاعات وحقل الكتابة |
| الارتفاع والتمرير | كما كانا: ٣٨٠×٥٦٠ ثابت، والسجل وحده يتمرّر — قِيسا بعد التغيير |

**نص المزود التجريبي:** حُذف من الرد الظاهر كل ذكر لـOracle و OCI و
`MODEL_PROVIDER=oracle`، وصار: «رد تجريبي — سيتم ربط مزود الذكاء الاصطناعي في
مرحلة الإعداد.» **ولم يُحذف أي كود Oracle من الـBackend** — التغيير في
`ai/mock_provider.py` وحده، وملفات `oracle_provider.py` و `embeddings.py`
و `database/` كما هي.

**خطأ حقيقي وُجد أثناء الصقل وأُصلح:** صفحة الدخول كانت تعرض «انتهت جلستك.
سجّل الدخول من جديد» عند كلمة مرور خاطئة، لأن `describeFailureDetail` تترجم
401 هكذا. على صفحة الدخول 401 تعني بيانات خاطئة لا جلسة منتهية — صارت تعرض
رسالة الـBackend كما هي، مثلما تفعل الإضافة منذ P4-01.

### ثالثًا: ملفات P4-04

| الملف | الحالة |
| ----- | ------ |
| `docs/MANUAL_TEST_CHECKLIST.md` | مضاف — ٥٠ خطوة مرقّمة بنتيجة متوقعة لكل خطوة |
| `docs/INSTALL_GUIDE.md` | مضاف — تشغيل من الصفر + أخطاء شائعة + ما يلزم قبل النشر |
| `docs/DEMO_SCRIPT.md` | مضاف — عرض ٨–١٠ دقائق + أسئلة متوقّعة + **ما لا يُدّعى** |
| `docs/BUG_REPORT_TEMPLATE.md` | مضاف — قالب + «ما ليس خطأً» + مثال معبّأ |
| `README.md` · `extension/README.md` | معدّلان |
| `frontend/app/globals.css` · `ChatPanel` · `Sidebar` · `SessionFooter` · `Workspace` · `ui/Button` · `app/login/page.tsx` | معدّلة |
| `extension/popup.css` · `extension/README.md` | معدّلان |
| `backend/app/ai/mock_provider.py` | معدّل — النص الظاهر فقط |

### رابعًا: الفحص الكامل — ما جُرّب فعلًا

| الفحص | النتيجة |
| ----- | ------- |
| `pytest` | **403 passed · 4 skipped · 0 failed** |
| `npm run build` | ✅ — TypeScript و٧ صفحات |
| `npx eslint .` | ✅ — بلا مخرجات، exit 0 |
| Newman كاملًا | **42/42 طلبًا · 99/99 تأكيدًا · 0 فشل** |
| الدخول والخروج | ✅ — والرمز يُمسح من التخزين |
| محادثة محفوظة | ✅ — تظهر في القائمة وتُستعاد باختيارها |
| رفع TXT و PDF و DOCX | ✅ — الثلاثة `processed` |
| ظهور `sources` | ✅ — `لائحة-الإجازات.txt` في مصادر الرد |
| لوحة المسؤول | ✅ — الاشتراك (٣ من ١٠) والموظفون وسجل التدقيق |
| اشتراك منتهٍ | ✅ — «انتهى اشتراك جهتك بتاريخ 2025-06-30…» |
| عزل جهتين | ✅ — **٩ محاولات** من الجهة (ب) على موارد (أ) كلها 404، والموارد سليمة بعدها |
| إضافة Chrome | ✅ — Chrome 151 |
| إضافة Edge | ✅ — Edge 151، الواجهة والإضافة معًا |
| Popup وTab | ✅ — ٣٨٠×٥٦٠ في النافذة، و٦٤٠ متوسّطة في التبويب (`left=400 right=1040` من ١٤٤٠) |
| أسرار أو ملفات مؤقتة | ✅ — لا `.env`، ولا مفاتيح، وكل الناتج مُدرَج في `.gitignore` |

**الفحص عبر الأجهزة:** جوال ٣٩٠×٨٤٤ ولوحي ٨٢٠×١١٨٠ ومكتبي ١٢٨٠ و١٤٤٠ —
**كلها بمحاكاة المتصفح لا بجهاز حقيقي**. لم يُستخدم أي جوال أو لوحي فعلي،
ولا Safari ولا iOS ولا Firefox ولا ماك.

**لم يُجرَّب إطلاقًا:** Azure · Oracle · OCI · `DATA_STORE=oracle` · أي مزود
مودل غير `mock`.

**تحذير باقٍ:** `StarletteDeprecationWarning` عن `httpx` من داخل
`fastapi.testclient` — خارج كودنا.

---

## الملخّص النهائي — PERSON_4

| المهمة | الحالة | أبرز ما فيها |
| ------ | ------ | ------------- |
| **P4-01** — الإضافة | ✅ Done | دخول وخروج، سجل محادثة، آخر محادثة، أربع حالات خطأ، صفر Content Scripts |
| **P4-02** — بيانات وPostman | ✅ Done | ٧ محادثات و٥ ملفات تجريبية، ومجموعة ٤٢ طلبًا تعمل بعد Clone مباشرة |
| **P4-03** — الاختبارات | ✅ Done | إغلاق الوضع الانتقالي في `/api/chat` + ١٥ اختبارًا جديدًا |
| **P4-04** — الفحص والتوثيق | ✅ Done | صقل بصري + ٤ وثائق + فحص كامل |

**حالة الفحوص الآلية عند التسليم:** pytest **403/4 skipped/0 failed** ·
build ✅ · eslint ✅ · Newman **42/42 · 99/99**.

**ما يلزم قبل أي استخدام حقيقي** (مفصّل في
[`INSTALL_GUIDE.md`](INSTALL_GUIDE.md#قبل-النشر-على-سيرفر-جهة)):
`JWT_SECRET` ثابت · `PROVISIONING_KEY` · `DATA_STORE=oracle` · مزود مودل
حقيقي · HTTPS.

> **متروك لقائد الفريق:** حفظ المحادثة المختارة بين تحديثات الصفحة في
> الواجهة (الإضافة تفعلها والواجهة لا) — مسجّل كتحسين في
> [`MANUAL_TEST_CHECKLIST.md`](MANUAL_TEST_CHECKLIST.md).

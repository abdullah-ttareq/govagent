# PERSON_1 — قائد الفريق · AI و RAG و Oracle والتكامل

**عدد المهام: ٦** — وهي أكبر وأصعب حصة في خطة الأسبوع.

هذا الملف جزء من **خطة الأسبوع الفعلية**، وهي أربعة ملفات:
[`PERSON_1.md`](PERSON_1.md) · [`PERSON_2.md`](PERSON_2.md) ·
[`PERSON_3.md`](PERSON_3.md) · [`PERSON_4.md`](PERSON_4.md).
مجموع مهام الخطة **٢٠ مهمة**. أما [`PROJECT_TASKS.md`](PROJECT_TASKS.md) فهو
Backlog مستقبلي وليس خطة هذا الأسبوع.

---

## قواعد إرشادية فقط

- التوزيع في هذه الملفات **اقتراح تنظيمي**، وليس ملكية.
- **أي عضو يستطيع تعديل أي ملف يحتاجه** لإنجاز مهمته.
- **قائد الفريق (PERSON_1) يستطيع نقل أي مهمة** بين الأعضاء في أي وقت.
- لا توجد ملكية إجبارية لأي مجلد، ولا موافقات، ولا قيود آلية.
- لا Issues ولا Branch Protection ولا CODEOWNERS ولا Git Hooks ولا CI Gates.
- **الاختبارات للتأكد والتسجيل فقط.** فشل اختبار لا يمنع حفظ العمل — سجّل
  النتيجة كما ظهرت واستمر.
- Claude لا ينفّذ `commit` أو `push` أو `merge`، ولا ينشئ أو يحذف فروعًا.
  أوامر Git تُنفَّذ يدويًا.

---

## ملخص إنجاز الشخص الأول

**المهام الست مكتملة** ضمن ما تسمح به البيئة المتاحة. التقرير النهائي الكامل
في [`docs/FINAL_REPORT.md`](FINAL_REPORT.md).

| المهمة | الحالة | المعلّق |
|---|---|---|
| P1-01 طبقة مزود المودل | ✅ | استدعاء OCI حي |
| P1-02 اتصال Oracle والمخطط | ✅ | تشغيل `schema.sql` على قاعدة حية |
| P1-03 استخراج النص والتقطيع | ✅ | الإدراج الفعلي في `document_chunks` |
| P1-04 البحث المتجهي و RAG | ✅ | `VECTOR_DISTANCE` على قاعدة حية |
| P1-05 Docker Compose | ✅ | `docker compose up` |
| P1-06 التكامل والتقرير | ✅ | — |

**اختبارات الـBackend:** `169 passed, 4 skipped` — لا اختبار فاشل. الأربعة
المتخطّاة كلها **Pending Oracle Setup**، وتُفعَّل بمجرد توفر قاعدة.

**يعمل اليوم بلا أي اعتماد خارجي:** محادثة عربية بسياق · استخراج نص من PDF و
DOCX و TXT · تقطيع يحترم الفقرات · متجهات محلية · بحث متجهي · عزل الجهات ·
مصادر في الرد · واجهة ويب · إضافة متصفح.

**⚠️ أهم ما تبقّى:** `organization_id` يصل من العميل لعدم وجود مصادقة. عزل
الجهات صحيح في كل طبقات التخزين والبحث، لكنه لا يُغني عن المصادقة. تُعالَج في
**BE-03 (الشخص الثاني)**: تُقرأ الجهة من التوكن ويُحذف الحقل من `ChatRequest`.
لا يُنشر النظام على شبكة مفتوحة قبل ذلك.

---

## دور قائد الفريق

بالإضافة إلى مهامه الست:

- يقرر ترتيب العمل ويعيد توزيع المهام عند الحاجة.
- يفكّ التعارض عندما يعدّل شخصان الملف نفسه.
- يقرر بشأن الملفات المشتركة (`.env.example` · `backend/app/main.py` ·
  `database/schema.sql` · `README.md`).
- ينفّذ مهمة التكامل النهائية (P1-06).

## كيف تنفّذ مهمة

كل مهمة تحتها **Prompt جاهز**. انسخه كما هو إلى Claude. البرومبت يفترض أن
Claude يعمل داخل `C:\Users\abdul\govagent-repo`.

---

# P1-01 — طبقة مزود المودل الكاملة

- **الحالة:** Done (باستثناء الاختبار الحي: **Pending Oracle Setup**)
- **ملاحظة:** لا توجد حتى الآن موارد ولا بيانات دخول في Oracle Cloud، فنُفّذ
  المزود والإعدادات كاملة دون أي اتصال حقيقي ودون أي أسرار في المستودع.
  الاختبار الحي مسجَّل كـSkip باسم
  `test_oracle_live_call_pending_oracle_setup` ويُفعَّل بعد تجهيز الحساب.
- **تعتمد على:** لا شيء
- **الملفات المتوقعة:** `backend/app/ai/` · `backend/app/services/chat_service.py`
  · `backend/app/schemas/chat.py` · `backend/tests/`

## ما تشمله

1. توسيع `ModelProvider` في `base.py` ليقبل **سياق المحادثة** (قائمة رسائل
   سابقة) بدل رسالة واحدة، مع إبقاء التوقيع بسيطًا.
2. تحديث `MockModelProvider` و `chat_service` تبعًا للتوقيع الجديد.
3. تنفيذ `OracleModelProvider` فعليًا فوق **OCI Generative AI**: قراءة
   `OCI_REGION` و `OCI_COMPARTMENT_ID` و `OCI_MODEL_ID`، وبناء الطلب، وقراءة
   الرد.
4. كتابة **System Prompt** حقيقي في `system_prompt.py` يحدد دور الإيجنت
   كمساعد عام لموظف حكومي: عربي رسمي، لا يخترع أنظمة أو أرقام قرارات، ويوضّح
   حدود معرفته.
5. **معالجة الأخطاء:** إعدادات ناقصة، فشل اتصال، مهلة، رد غير صالح — كلها
   ترفع `ModelProviderError` برسالة عربية واضحة تصل للمستخدم كـ503.

## شرط الإنجاز

- المشروع يعمل بـ`MODEL_PROVIDER=mock` **بدون حزمة `oci` مثبتة** إطلاقًا.
- `MODEL_PROVIDER=oracle` بإعدادات ناقصة يعطي رسالة واضحة تذكر المتغير الناقص.
- `pytest` يمر (سجّل النتيجة كما ظهرت).

```
اقرأ docs/PERSON_1.md.
نفّذ المهمة P1-01. تأكد من أنك داخل C:\Users\abdul\govagent-repo عبر
git rev-parse --show-toplevel قبل أي تعديل.
افحص backend/app/ai/ كاملًا قبل التعديل. لا تعِد إنشاء المشروع.
لا تعمل Commit أو Push أو Merge، ولا تنشئ فروعًا أو Issues.

المطلوب:
1. وسّع ModelProvider في base.py ليقبل سياق المحادثة (قائمة رسائل سابقة) بدل
   رسالة واحدة، مع توقيع بسيط. حدّث MockModelProvider و chat_service.
2. نفّذ OracleModelProvider فوق OCI Generative AI باستخدام OCI_REGION و
   OCI_COMPARTMENT_ID و OCI_MODEL_ID. اجعل استيراد حزمة oci كسولًا داخل
   الدالة حتى يبقى المشروع يعمل بـmock بدون تثبيتها.
3. اكتب System Prompt حقيقيًا في system_prompt.py: عربي رسمي، مساعد عام
   لموظف حكومي، لا يخترع أنظمة ولا أرقام قرارات، ويوضّح حدود معرفته.
4. اجعل كل أخطاء المزود (إعداد ناقص، فشل اتصال، مهلة، رد غير صالح) ترفع
   ModelProviderError برسالة عربية واضحة تظهر للمستخدم كـ503.
5. أضف اختبارات في backend/tests/ تغطي: mock يعمل، oracle بإعداد ناقص يرمي
   خطأ واضح، ومزود غير مدعوم يرمي خطأ يذكر القيم المقبولة.

شغّل: cd backend && .venv/Scripts/python -m pytest
في النهاية اذكر الملفات المعدّلة ونتائج الاختبارات كما ظهرت فعلًا.
```

---

# P1-02 — Oracle: الاتصال وتشغيل المخطط

- **الحالة:** Done (باستثناء تشغيل المخطط فعليًا: **Pending Oracle Setup**)
- **ملاحظة:** لا توجد حتى الآن قاعدة Oracle ولا بيانات دخول، فنُفّذت طبقة
  الاتصال والمخطط والتوثيق كاملة دون أي اتصال حقيقي ودون أي أسرار في
  المستودع. تشغيل `schema.sql` على قاعدة فعلية مسجَّل كـSkip باسم
  `test_schema_runs_on_a_clean_database_pending_oracle_setup`. رُوجع المخطط
  مراجعة ثابتة (ترتيب المفاتيح الأجنبية، الأنواع، الفهارس) ولم يُنفَّذ على
  قاعدة حية.
- **تعتمد على:** لا شيء (يمكن أن تسير بالتوازي مع P1-01)
- **الملفات المتوقعة:** `backend/app/database/` · `database/schema.sql` ·
  `.env.example` · `backend/requirements.txt`

## ما تشمله

1. طبقة اتصال Oracle في `backend/app/database/`: تهيئة Pool من `ORACLE_DSN`
   و `ORACLE_USER` و `ORACLE_PASSWORD`، ودالة تعطي اتصالًا، وإغلاق نظيف عند
   إيقاف التطبيق.
2. **الاتصال كسول (lazy):** المشروع يجب أن يقلع ويعمل بالكامل بـ`mock` وبحقول
   Oracle فارغة. لا محاولة اتصال إلا عند أول استخدام فعلي.
3. مراجعة `database/schema.sql` وتشغيله فعليًا على قاعدة Oracle، وإصلاح أي
   خطأ في الأنواع أو المفاتيح أو ترتيب الجداول.
4. توثيق خطوات التشغيل: كيف تُنشأ القاعدة، وكيف يُشغَّل المخطط، وماذا يفعل من
   لا يملك Oracle.
5. إضافة فحص اتصال اختياري إلى `/health` **لا يُفشل الفحص** إذا كانت Oracle
   غير مضبوطة.

## شرط الإنجاز

- `uvicorn` يقلع بحقول Oracle فارغة بلا أي خطأ.
- `schema.sql` يعمل من أوله إلى آخره على قاعدة نظيفة.
- خطأ الاتصال (كلمة مرور خاطئة مثلًا) يعطي رسالة واضحة لا Stack Trace خامًا.

```
اقرأ docs/PERSON_1.md.
نفّذ المهمة P1-02. تأكد من أنك داخل C:\Users\abdul\govagent-repo عبر
git rev-parse --show-toplevel قبل أي تعديل.
لا تعمل Commit أو Push أو Merge، ولا تنشئ فروعًا أو Issues.

المطلوب:
1. اكتب طبقة اتصال Oracle في backend/app/database/ باستخدام oracledb:
   Connection Pool من ORACLE_DSN و ORACLE_USER و ORACLE_PASSWORD، دالة تعطي
   اتصالًا، وإغلاق نظيف عند إيقاف التطبيق.
2. اجعل الاتصال كسولًا: التطبيق يقلع ويعمل كاملًا بـMODEL_PROVIDER=mock وحقول
   Oracle فارغة، ولا يحاول الاتصال إلا عند أول استخدام فعلي.
3. راجع database/schema.sql: صحّح الأنواع والمفاتيح وترتيب الجداول ليعمل من
   أوله إلى آخره على قاعدة نظيفة.
4. أضف إلى /health حقلًا اختياريًا يبيّن حالة Oracle، بحيث لا يفشل الفحص إذا
   كانت غير مضبوطة.
5. وثّق في database/ خطوات إنشاء القاعدة وتشغيل المخطط، وماذا يفعل من لا
   يملك Oracle.
أضف oracledb إلى backend/requirements.txt.

شغّل: cd backend && .venv/Scripts/python -m pytest
وتأكد أن uvicorn يقلع بحقول Oracle فارغة.
في النهاية اذكر الملفات المعدّلة ونتائج الاختبارات كما ظهرت فعلًا.
```

---

# P1-03 — استخراج نص الملفات والتقطيع

- **الحالة:** Done (باستثناء الحفظ الفعلي في Oracle: **Pending Oracle Setup**)
- **ملاحظة:** نُفِّذ المسار كاملًا (استخراج ← تقطيع ← متجهات ← حفظ) ويعمل بلا
  Oracle حية: التقطيع دالة نقية، والمتجهات لها مزود محلي ثابت
  (`EMBEDDING_PROVIDER=mock`) لا يحتاج أي خدمة خارجية، والحفظ مُختبَر باتصال
  وهمي يثبت أن `organization_id` يدخل في كل عبارة SQL. الإدراج الفعلي في
  `document_chunks` مسجَّل كـSkip باسم
  `test_chunks_are_persisted_in_oracle_pending_oracle_setup`.
- **تعتمد على:** P1-02
- **الملفات المتوقعة:** `backend/app/services/` · `backend/app/database/` ·
  `backend/tests/`

## ما تشمله

1. استخراج النص من الملفات المرفوعة: **PDF و DOCX و TXT** على الأقل.
2. رفض الامتدادات غير المدعومة برسالة عربية واضحة، وحد أقصى معقول للحجم.
3. تقطيع النص إلى **Chunks** بحجم ثابت مع تداخل بسيط، مع الحفاظ على حدود
   الفقرات قدر الإمكان.
4. حفظ الـChunks في جدول `document_chunks` مربوطة بالملف وبـ`organization_id`.
5. توليد **Embeddings** لكل Chunk وتخزينها استعدادًا للبحث في P1-04.

## شرط الإنجاز

- ملف PDF تجريبي يُستخرج نصه ويُقطّع ويُحفظ.
- ملف بامتداد غير مدعوم يعطي خطأ واضحًا لا انهيارًا.
- اختبار يغطي التقطيع بنص ثابت بلا حاجة إلى Oracle.

```
اقرأ docs/PERSON_1.md.
نفّذ المهمة P1-03. تأكد من أنك داخل C:\Users\abdul\govagent-repo عبر
git rev-parse --show-toplevel قبل أي تعديل.
لا تعمل Commit أو Push أو Merge، ولا تنشئ فروعًا أو Issues.

المطلوب:
1. اكتب خدمة استخراج نص من PDF و DOCX و TXT في backend/app/services/.
   ارفض الامتدادات غير المدعومة برسالة عربية واضحة، وضع حدًا أقصى للحجم.
2. اكتب دالة تقطيع (chunking) بحجم ثابت مع تداخل بسيط، تحترم حدود الفقرات
   قدر الإمكان. اجعلها دالة نقية قابلة للاختبار بلا Oracle.
3. احفظ الـChunks في جدول document_chunks مربوطة بالملف وبـorganization_id.
4. ولّد Embedding لكل Chunk وخزّنه استعدادًا للبحث المتجهي.
5. أضف اختبارات للتقطيع ولرفض الامتدادات غير المدعومة.

شغّل: cd backend && .venv/Scripts/python -m pytest
في النهاية اذكر الملفات المعدّلة ونتائج الاختبارات كما ظهرت فعلًا.
```

---

# P1-04 — RAG: البحث المتجهي وربطه بالمحادثة

- **الحالة:** Done (باستثناء تنفيذ VECTOR_DISTANCE على قاعدة حية:
  **Pending Oracle Setup**)
- **ملاحظة:** الـRAG يعمل كاملًا بلا Oracle عبر مخزن محلي داخل الذاكرة
  (`RETRIEVAL_PROVIDER=memory`) مع `MockEmbeddingProvider`. Oracle Vector
  Search منفَّذ وجاهز للتفعيل بتغيير `RETRIEVAL_PROVIDER=oracle` بعد تجهيز
  القاعدة، واستعلامه مُختبَر باتصال وهمي يثبت شرط `organization_id` مرتين.
  التنفيذ الفعلي على قاعدة حية مسجَّل كـSkip باسم
  `test_oracle_vector_search_pending_oracle_setup`.
- **قيدان معروفان:** (١) المخزن المحلي غير دائم ويفرغ عند إعادة تشغيل
  الـBackend، وهو للتطوير والعرض لا للإنتاج. (٢) المتجهات المحلية تعتمد على
  الكلمات فلا تحل الضمائر في الأسئلة المتتابعة؛ مزود OCI يتجاوز ذلك.
- **⚠️ أمني مؤقت:** `organization_id` يصل حاليًا من العميل في جسم الطلب لعدم
  وجود تسجيل دخول بعد. **يجب** أن يُقرأ من التوكن ويُحذف من `ChatRequest` عند
  تنفيذ BE-03، وإلا أمكن لأي عميل طلب مقاطع جهة أخرى.
- **تعتمد على:** P1-01 · P1-03
- **الملفات المتوقعة:** `backend/app/services/` · `backend/app/database/` ·
  `backend/app/api/chat.py` · `backend/tests/`

## ما تشمله

1. **Oracle Vector Search** فوق `document_chunks`: استعلام أقرب الجيران يعيد
   أعلى `k` مقاطع.
2. **عزل صارم:** البحث لا يتجاوز أبدًا `organization_id` الخاص بالمستخدم.
   هذه أخطر نقطة في المشروع كله.
3. ربط النتيجة بمسار المحادثة: جلب المقاطع، تركيبها في سياق مضغوط، وتمريرها
   إلى المزود مع سؤال المستخدم.
4. إعادة **مصادر الرد** في `ChatResponse` (اسم الملف ورقم المقطع) حتى تعرضها
   الواجهة.
5. مسار بديل نظيف: إن لم توجد ملفات أو كانت Oracle غير مضبوطة، يجيب الإيجنت
   من معرفته العامة بلا انهيار.

## شرط الإنجاز

- سؤال عن محتوى ملف مرفوع يعيد ردًا يذكر مصدره.
- مستخدم من جهة (أ) لا يرى مطلقًا مقاطع جهة (ب) — يجب إثبات ذلك باختبار.
- المحادثة تعمل بلا ملفات مرفوعة إطلاقًا.

```
اقرأ docs/PERSON_1.md.
نفّذ المهمة P1-04. تأكد من أنك داخل C:\Users\abdul\govagent-repo عبر
git rev-parse --show-toplevel قبل أي تعديل.
لا تعمل Commit أو Push أو Merge، ولا تنشئ فروعًا أو Issues.

المطلوب:
1. نفّذ بحث Oracle Vector Search فوق document_chunks يعيد أعلى k مقاطع
   للسؤال.
2. اجعل كل استعلام بحث مقيّدًا بـorganization_id الخاص بالمستخدم دائمًا. لا
   يوجد مسار واحد يتجاوز هذا القيد.
3. اربط النتيجة بمسار /api/chat: اجلب المقاطع، ركّبها في سياق مضغوط، ومررها
   إلى مزود المودل مع سؤال المستخدم.
4. أضف حقل sources إلى ChatResponse يذكر اسم الملف ورقم المقطع لكل مصدر.
5. إن لم توجد ملفات أو كانت Oracle غير مضبوطة، يجب أن يجيب الإيجنت من معرفته
   العامة بلا انهيار وبلا خطأ.
6. أضف اختبارًا يثبت أن مستخدم جهة أ لا يحصل على أي مقطع من جهة ب.

شغّل: cd backend && .venv/Scripts/python -m pytest
في النهاية اذكر الملفات المعدّلة ونتائج الاختبارات كما ظهرت فعلًا.
```

---

# P1-05 — التشغيل الذاتي: Docker Compose والإعداد

- **الحالة:** Done (باستثناء تجربة `docker compose up`: **لم تُجرَّب**)
- **ملاحظة:** Docker غير مثبّتة على جهاز التطوير، فلم يُنفَّذ
  `docker compose up` فعليًا. ما جُرِّب بالفعل: بناء الواجهة
  (`npm run build`) نجح، وأمر تشغيلها كما هو في الـDockerfile يعمل ويعيد
  HTTP 200، و`docker-compose.yml` صالح نحويًا ولا يحتوي خدمة Oracle. بناء
  الصور وتشغيلها معًا يحتاج سيرفرًا فيه Docker.
- **تكامل Oracle الحقيقي:** Pending Oracle Setup كما هو.
- **تعتمد على:** P1-02
- **الملفات المتوقعة:** `docker-compose.yml` · `backend/Dockerfile` ·
  `frontend/Dockerfile` · `.env.example` · `docs/`

## ما تشمله

1. `Dockerfile` للـBackend وآخر للـFrontend، بأبسط صورة تعمل.
2. `docker-compose.yml` يشغّل الخدمتين معًا ويمرر متغيرات البيئة من `.env`.
3. Oracle **خارجية** وليست داخل Compose — الجهة تملك قاعدتها.
4. توثيق **إعداد التشغيل الذاتي** للجهة: ما الذي تحتاجه، وما الذي تضبطه، وما
   الذي يبقى فارغًا.
5. تحديث `.env.example` بأي متغير جديد، **بقيم فارغة دائمًا**.

## شرط الإنجاز

- `docker compose up` يشغّل Backend و Frontend ويعملان معًا بـ`mock`.
- لا يوجد أي سر أو بيانات اعتماد داخل أي ملف مرفوع.

```
اقرأ docs/PERSON_1.md.
نفّذ المهمة P1-05. تأكد من أنك داخل C:\Users\abdul\govagent-repo عبر
git rev-parse --show-toplevel قبل أي تعديل.
لا تعمل Commit أو Push أو Merge، ولا تنشئ فروعًا أو Issues.

المطلوب:
1. اكتب backend/Dockerfile و frontend/Dockerfile بأبسط صورة تعمل.
2. اكتب docker-compose.yml يشغّل الخدمتين ويمرر متغيرات البيئة من .env.
   لا تضع Oracle داخل Compose — القاعدة خارجية تملكها الجهة.
3. تأكد أن التشغيل الافتراضي يعمل بـMODEL_PROVIDER=mock بلا Oracle.
4. حدّث .env.example بأي متغير جديد، بقيم فارغة فقط.
5. أضف ملف توثيق قصيرًا في docs/ يشرح للجهة كيف تشغّل النظام على سيرفرها:
   المتطلبات، وما تضبطه، وما يبقى فارغًا.

في النهاية اذكر الملفات المعدّلة، وما جرّبته فعليًا وما لم تجرّبه.
```

---

# P1-06 — التكامل والاختبار الشامل والنشر

- **الحالة:** Done (ضمن المتاح فعليًا: mock + memory، بلا Oracle وبلا Docker)
- **المخرجات:** [`docs/FINAL_REPORT.md`](FINAL_REPORT.md) ·
  [`docs/HOSTING_DECISION.md`](HOSTING_DECISION.md)
- **ما جُرِّب فعليًا:** إقلاع الـBackend · `/health` · محادثة عادية عبر HTTP
  حي · سياق المحادثة · معالجة ملف DOCX حقيقي و RAG · ظهور المصادر · عزل
  الجهات · رفض الامتدادات · العمل بلا Oracle · **الواجهة مع الـBackend في
  متصفح حقيقي** (ظهر الرد في الواجهة) · توافق الإضافة مع الـAPI.
- **ما لم يُجرَّب ولا يُدّعى اختباره:** Oracle (اتصال، `schema.sql`،
  `VECTOR_DISTANCE`) · OCI Generative AI · `docker compose up` · تحميل
  الإضافة داخل Chrome · مسار من تسجيل الدخول (لا مصادقة بعد).
- **قرار الاستضافة:** كان **غير محسوم** قبل هذه المهمة. التوصية: **سيرفر
  الجهة** للتطبيق، مع OCI اختياريًا لمزود المودل وحده.
- **تعتمد على:** كل المهام السابقة ومهام بقية الأعضاء
- **الملفات المتوقعة:** أي ملف يحتاجه التكامل · `docs/`

## ما تشمله

هذه مهمة قائد الفريق النهائية، وتُنفَّذ آخر الأسبوع:

1. تجميع عمل الأعضاء الأربعة وحل التعارضات.
2. تشغيل **النظام كاملًا**: Backend + Frontend + Extension معًا، ومرور مسار
   كامل من تسجيل الدخول إلى رد الإيجنت.
3. تشغيل كل الاختبارات وتسجيل النتيجة الفعلية — **بما فيها الفاشلة**. الفشل
   يُسجَّل ولا يُخفى ولا يمنع التسليم.
4. مراجعة أمنية سريعة: لا أسرار مرفوعة، كلمات المرور مُجزّأة، وعزل
   `organization_id` قائم في كل مسار.
5. **قرار الاستضافة:** المقارنة بين OCI و VPS وسيرفر الجهة، والتوصية. القرار
   **لم يُحسم بعد**، وهذه المهمة هي التي تحسمه وتوثّقه.
6. تقرير نهائي في `docs/`: ما اكتمل، وما لم يكتمل، والأخطاء المعروفة المتبقية.

## شرط الإنجاز

- مسار كامل يعمل من الواجهة ومن الإضافة.
- تقرير نهائي مكتوب فيه نتائج الاختبارات كما ظهرت.

```
اقرأ docs/PERSON_1.md و docs/PERSON_2.md و docs/PERSON_3.md و docs/PERSON_4.md.
نفّذ المهمة P1-06. تأكد من أنك داخل C:\Users\abdul\govagent-repo عبر
git rev-parse --show-toplevel قبل أي تعديل.
لا تعمل Commit أو Push أو Merge، ولا تنشئ فروعًا أو Issues. أوامر Git يدوية.

المطلوب:
1. شغّل النظام كاملًا: Backend ثم Frontend ثم الإضافة، وجرّب مسارًا كاملًا من
   تسجيل الدخول حتى رد الإيجنت.
2. شغّل كل الاختبارات الموجودة وسجّل النتيجة الفعلية، بما فيها الفاشلة. لا
   تخفِ أي فشل ولا تعطّل أي اختبار.
3. راجع أمنيًا: لا أسرار في المستودع، كلمات المرور مُجزّأة، وعزل
   organization_id مطبّق في كل مسار قراءة وكتابة.
4. اكتب في docs/ مقارنة قصيرة لخيارات الاستضافة (OCI مقابل VPS مقابل سيرفر
   الجهة) مع توصية واضحة، واذكر صراحة أن القرار كان غير محسوم قبل هذه المهمة.
5. اكتب تقريرًا نهائيًا في docs/: ما اكتمل، وما لم يكتمل، والأخطاء المعروفة
   المتبقية.

في النهاية اذكر الملفات المعدّلة ونتائج الاختبارات كما ظهرت فعلًا.
```

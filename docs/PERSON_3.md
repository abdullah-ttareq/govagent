# PERSON_3 — واجهة الويب (Web Frontend)

**عدد المهام: ٥**

هذا الملف جزء من **خطة الأسبوع الفعلية**، وهي أربعة ملفات:
[`PERSON_1.md`](PERSON_1.md) · [`PERSON_2.md`](PERSON_2.md) ·
[`PERSON_3.md`](PERSON_3.md) · [`PERSON_4.md`](PERSON_4.md).
مجموع مهام الخطة **٢٠ مهمة**. أما [`PROJECT_TASKS.md`](PROJECT_TASKS.md) فهو
Backlog مستقبلي وليس خطة هذا الأسبوع.

**قائد الفريق هو PERSON_1.**

---

## قواعد إرشادية فقط

- التوزيع في هذه الملفات **اقتراح تنظيمي**، وليس ملكية.
- **أي عضو يستطيع تعديل أي ملف يحتاجه** لإنجاز مهمته.
- **قائد الفريق يستطيع نقل أي مهمة** بين الأعضاء في أي وقت.
- لا توجد ملكية إجبارية لأي مجلد، ولا موافقات، ولا قيود آلية.
- لا Issues ولا Branch Protection ولا CODEOWNERS ولا Git Hooks ولا CI Gates.
- **الاختبارات للتأكد والتسجيل فقط.** فشل اختبار لا يمنع حفظ العمل.
- Claude لا ينفّذ `commit` أو `push` أو `merge`، ولا ينشئ أو يحذف فروعًا.

## ملاحظات تخص الواجهة

- المشروع **Next.js App Router** موجود فعلًا في `frontend/`. **لا تعِد إنشاءه**
  ولا تبدّل إطار العمل.
- الواجهة **عربية RTL** بالكامل.
- إن لم تكن مسارات الـBackend جاهزة بعد، اعمل على بيانات تجريبية في
  `frontend/lib/mock-conversations.ts` واربط لاحقًا في P3-05.
- **إضافة المتصفح في `extension/` ليست ضمن مهامك** — هي في
  [`PERSON_4.md`](PERSON_4.md)، وهي HTML و CSS و JS مباشر بلا React.

---

# P3-01 — نظام التصميم و RTL وصفحة الدخول

- **الحالة:** Done
- **تعتمد على:** لا شيء
- **الملفات المتوقعة:** `frontend/app/` · `frontend/components/` ·
  `frontend/app/globals.css`

## ما تشمله

1. **نظام تصميم بسيط:** ألوان وخطوط ومسافات وأحجام نص كمتغيرات CSS، وأزرار
   وحقول إدخال ورسائل موحّدة.
2. تثبيت **RTL** في كل الواجهة: `dir="rtl"` و `lang="ar"`، ومحاذاة وأيقونات
   صحيحة الاتجاه.
3. خط عربي واضح ومقروء على الشاشات الصغيرة.
4. **صفحة تسجيل الدخول:** حقل بريد وكلمة مرور، وتحقق أولي في الواجهة، وحالة
   إرسال، ورسالة خطأ عربية واضحة عند فشل الدخول.
5. حفظ الـToken وحماية الصفحات: غير المسجَّل يُحوَّل إلى صفحة الدخول.

## شرط الإنجاز

- الواجهة كلها RTL بلا نص مقلوب أو محاذاة خاطئة.
- زيارة صفحة محمية بلا تسجيل دخول تُحوِّل إلى `/login`.

```
اقرأ docs/PERSON_3.md.
نفّذ المهمة P3-01. تأكد من أنك داخل C:\Users\abdul\govagent-repo عبر
git rev-parse --show-toplevel قبل أي تعديل.
افحص frontend/ كاملًا قبل التعديل. لا تعِد إنشاء المشروع ولا تبدّل إطار العمل.
لا تعمل Commit أو Push أو Merge، ولا تنشئ فروعًا أو Issues.

المطلوب:
1. أنشئ نظام تصميم بسيطًا: ألوان وخطوط ومسافات وأحجام نص كمتغيرات CSS، مع
   أزرار وحقول إدخال ورسائل موحّدة قابلة لإعادة الاستخدام.
2. ثبّت RTL في كل الواجهة: dir="rtl" و lang="ar"، ومحاذاة واتجاه أيقونات صحيح.
3. اختر خطًا عربيًا واضحًا ومقروءًا على الشاشات الصغيرة.
4. أنشئ صفحة تسجيل دخول: بريد وكلمة مرور، تحقق أولي في الواجهة، حالة إرسال،
   ورسالة خطأ عربية واضحة عند الفشل.
5. احفظ الـToken بعد الدخول، واحمِ الصفحات: غير المسجَّل يُحوَّل إلى /login.
إن لم يكن مسار تسجيل الدخول جاهزًا في الـBackend، اعمل على بيانات تجريبية
واترك نقطة الربط واضحة.

شغّل: cd frontend && npm run build
في النهاية اذكر الملفات المعدّلة ونتيجة البناء كما ظهرت فعلًا.
```

## النتيجة

**الحالة: Done** — نُفِّذت على فرع `frontend`.

### الملفات

**جديدة**

| الملف | الدور |
| ----- | ----- |
| `frontend/app/login/page.tsx` | صفحة تسجيل الدخول |
| `frontend/lib/auth.ts` | نداءات `/api/auth` وتخزين الرمز والتحقق الأولي |
| `frontend/components/AuthProvider.tsx` | سياق الجلسة: `signIn` و `signOut` والتحقق عند الإقلاع |
| `frontend/components/RequireAuth.tsx` | غلاف الصفحات المحمية |
| `frontend/components/SessionFooter.tsx` | بطاقة الموظف وزر الخروج |
| `frontend/components/ui/Button.tsx` | زر موحّد بأربع نبرات وحالة إرسال |
| `frontend/components/ui/Input.tsx` | حقل موحّد بتسمية وخطأ مرتبط بـ`aria-describedby` |
| `frontend/components/ui/Alert.tsx` | رسالة موحّدة بأربع نبرات |

**معدَّلة**

| الملف | التعديل |
| ----- | ------- |
| `frontend/app/globals.css` | نظام التصميم كاملًا + مكوّنات `.gv-*` |
| `frontend/app/layout.tsx` | أوزان خط Cairo و `display: swap`، `viewport`، تغليف بـ`AuthProvider` |
| `frontend/app/page.tsx` | تغليف الصفحة الرئيسية بـ`RequireAuth` |
| `frontend/lib/api.ts` | `apiRequest` موحّد + `ApiError` و `NetworkError` + ترويسة `Bearer` |
| `frontend/components/ChatPanel.tsx` | إرسال الرمز مع الرسالة، ورسائل خطأ عربية، ومكوّنات النظام |
| `frontend/components/Sidebar.tsx` | خصائص منطقية (`border-e` و `text-start`) وبطاقة الجلسة |
| `frontend/components/MessageBubble.tsx` | `break-words` وتوثيق منطقية المحاذاة |

**لم تُعدَّل أي ملفات مشتركة** من قائمة `TEAM_RULES.md` قسم ٦، **ولم تُضف أي
مكتبة جديدة** — `package.json` كما هو.

### الربط بالـBackend

على المسارات الموجودة فعلًا في `backend/app/api/auth.py`، بلا أي API مخترعة
وبلا بيانات تجريبية للمصادقة:

- `POST /api/auth/login` → حفظ `access_token`
- `GET /api/auth/me` → التحقق من الرمز المحفوظ عند فتح التطبيق
- `POST /api/auth/logout` → عند الخروج

`mock-conversations.ts` باقٍ كما هو — قائمة المحادثات من نصيب P3-02.

### نتيجة البناء

`cd frontend && npm run build` — **نجح**:

```
▲ Next.js 16.3.2 (Turbopack)
✓ Compiled successfully in 15.1s
  Finished TypeScript in 2.7s
✓ Generating static pages using 6 workers (5/5) in 938ms

Route (app)
┌ ○ /
├ ○ /_not-found
└ ○ /login
```

### الاختبار الفعلي

الـBackend على `MODEL_PROVIDER=mock` و `DATA_STORE=memory`، والواجهة على
`npm run dev`، والتجربة في المتصفح:

| ما جُرِّب | النتيجة |
| -------- | ------- |
| فتح `/` بلا رمز | حُوِّل إلى `/login`، و`localStorage` فارغ |
| دخول بكلمة مرور خاطئة | «البريد الإلكتروني أو كلمة المرور غير صحيحة» (401) |
| دخول صحيح `admin@digital-services.test` | فُتحت `/`، والرمز محفوظ، والشريط يعرض «سارة العتيبي · هيئة الخدمات الرقمية · مسؤول الجهة» |
| دخول بجهة منتهية `admin@national-archive.test` | «انتهى اشتراك جهتك بتاريخ 2025-06-30…» (403) — الرسالة من الـBackend بتاريخها الفعلي |
| بريد بصيغة خاطئة | خطأ تحت الحقل ولم يُرسَل طلب أصلًا |
| `dir` و `lang` على `<html>` | `rtl` و `ar` |
| عرض ٣٦٠ بكسل | التخطيط سليم بلا تمرير أفقي |
| أخطاء JavaScript في الـConsole | لا شيء — الظاهر فقط سجل المتصفح لردّي 401 و 403 المتوقَّعَين |

**ما لم يُجرَّب:** تسجيل الخروج من داخل التطبيق، والسلوك عند توقّف الـBackend
أثناء جلسة قائمة. الأول مباشر، والثاني تغطيه P3-03 (حالات الأخطاء).

### قرارات وملاحظات للمراجعة

- **الرمز في `localStorage`.** اختيار MVP واعٍ: البديل الصحيح كوكي
  `HttpOnly` يضبطه السيرفر، والـBackend اليوم يعيد الرمز في جسم الرد. نقطة
  التبديل محصورة في ثلاث دوال في `lib/auth.ts`.
- **`RequireAuth` حماية عرض لا حماية وصول.** الحاجز الحقيقي هو فحص الرمز في
  الـBackend عند كل طلب محمي؛ ما في الواجهة يمنع وميض صفحة فارغة لمن لا
  جلسة له.
- **انقطاع الشبكة لا يُحذف عنده الرمز.** فشل `/api/auth/me` بسبب سيرفر
  متوقف يعني «لا نعرف»، لا «الجلسة منتهية»؛ حذف الرمز حينها يجبر المستخدم
  على دخول جديد لن ينجح أصلًا. يُحذف الرمز عند 401 و 403 وحدهما.
- **حقلا البريد وكلمة المرور بـ`dir="ltr"`** داخل واجهة RTL: العنوان اللاتيني
  يُقلب موضع `@` والنقطة بصريًا لو عُرض RTL.
- **أصغر حجم نص ١٣px وحقول الإدخال ١٦px:** الأول لأن العربية تفقد وضوح
  النقاط تحته، والثاني لأن Safari يكبّر الصفحة تلقائيًا عند التركيز على حقل
  أصغر من ١٦px.

---

# P3-02 — واجهة الشات والمحادثات

- **الحالة:** Done
- **تعتمد على:** P3-01
- **الملفات المتوقعة:** `frontend/app/` · `frontend/components/` ·
  `frontend/lib/`

## ما تشمله

1. **واجهة الشات:** قائمة رسائل تفرّق بصريًا بين رسالة المستخدم ورد الإيجنت،
   وحقل إدخال متعدد الأسطر، وإرسال بـ`Enter` وسطر جديد بـ`Shift+Enter`.
2. تمرير تلقائي إلى آخر رسالة، ونص طويل يلتف بلا كسر التخطيط.
3. **الشريط الجانبي (Sidebar):** قائمة المحادثات، والمحادثة النشطة مميّزة،
   وقابل للطي على الشاشات الصغيرة.
4. **إنشاء محادثة جديدة، وإعادة تسميتها، وحذفها** مع تأكيد قبل الحذف
   (تأكيد داخل الواجهة، لا `confirm()` المتصفح).
5. حالة فارغة واضحة عند عدم وجود محادثات.

## شرط الإنجاز

- إرسال رسالة يعرض رد الإيجنت في نفس المحادثة.
- إنشاء وإعادة تسمية وحذف محادثة تعمل وتنعكس في الشريط الجانبي فورًا.

```
اقرأ docs/PERSON_3.md.
نفّذ المهمة P3-02. تأكد من أنك داخل C:\Users\abdul\govagent-repo عبر
git rev-parse --show-toplevel قبل أي تعديل.
لا تعمل Commit أو Push أو Merge، ولا تنشئ فروعًا أو Issues.

المطلوب:
1. طوّر واجهة الشات: قائمة رسائل تفرّق بصريًا بين رسالة المستخدم ورد الإيجنت،
   وحقل إدخال متعدد الأسطر، وإرسال بـEnter وسطر جديد بـShift+Enter.
2. أضف تمريرًا تلقائيًا إلى آخر رسالة، واجعل النص الطويل يلتف بلا كسر التخطيط.
3. طوّر الشريط الجانبي: قائمة المحادثات، تمييز المحادثة النشطة، وقابلية الطي
   على الشاشات الصغيرة.
4. أضف إنشاء محادثة جديدة وإعادة تسميتها وحذفها، مع تأكيد للحذف داخل الواجهة
   وليس عبر confirm() المتصفح.
5. أضف حالة فارغة واضحة عند عدم وجود محادثات.

شغّل: cd frontend && npm run build
في النهاية اذكر الملفات المعدّلة ونتيجة البناء كما ظهرت فعلًا.
```

## النتيجة

**الحالة: Done** — نُفِّذت على فرع `frontend`.

### الملفات

**جديدة**

| الملف | الدور |
| ----- | ----- |
| `frontend/components/Workspace.tsx` | مالك حالة المحادثات والمحادثة النشطة، ومترجم الأخطاء المشترك |
| `frontend/components/ConversationItem.tsx` | صفّ محادثة بأزرار التسمية والحذف |
| `frontend/components/ui/Modal.tsx` | نافذة حوارية داخل الواجهة |
| `frontend/components/ui/ConfirmDialog.tsx` | تأكيد الإجراءات المدمّرة |
| `frontend/lib/conversations.ts` | نداءات `/api/conversations` والتحقق من العنوان |
| `frontend/lib/format.ts` | صياغة الوقت النسبي بالعربية |

**معدَّلة**

| الملف | التعديل |
| ----- | ------- |
| `frontend/components/ChatPanel.tsx` | تحميل الرسائل المحفوظة، Enter/Shift+Enter، تمرير تلقائي، حقل يكبر مع النص |
| `frontend/components/Sidebar.tsx` | قائمة حقيقية، تمييز النشطة، درج على الجوال، إنشاء وتسمية وحذف |
| `frontend/components/MessageBubble.tsx` | (بلا تغيير جوهري — `break-words` من P3-01) |
| `frontend/app/page.tsx` | `RequireAuth` يغلّف `Workspace` |
| `frontend/app/globals.css` | أنماط الشريط والدرج وصفّ المحادثة والنافذة والهيكل |
| `frontend/lib/api.ts` | `sendChatMessage` يقبل `conversation_id`، ورد يحمل `sources` |

**محذوف:** `frontend/lib/mock-conversations.ts` — صار بلا مراجع بعد الربط
بالـAPI الحقيقي. (تُتابَع بقية البيانات التجريبية في P3-05.)

**لم تُعدَّل أي ملفات مشتركة، ولم تُضف أي مكتبة** — `package.json` كما هو.

### الربط بالـBackend

على مسارات `backend/app/api/conversations.py` الموجودة فعلًا:

| المسار | الاستخدام |
| ------ | --------- |
| `GET /api/conversations` | قائمة الشريط الجانبي |
| `POST /api/conversations` | زر «محادثة جديدة» |
| `PATCH /api/conversations/{id}` | إعادة التسمية |
| `DELETE /api/conversations/{id}` | الحذف بعد التأكيد |
| `GET /api/conversations/{id}/messages` | تحميل رسائل المحادثة عند اختيارها |
| `POST /api/chat` | الإرسال، مع `conversation_id` أو بدونه لفتح محادثة جديدة |

**`history` لا يُرسل إطلاقًا:** مع رمز الدخول يقرأ الـBackend السياق من
المحادثة المحفوظة، ويرفض سياق العميل بـ422.

### نتيجة البناء والفحص

`cd frontend && npm run build` — **نجح**:

```
▲ Next.js 16.3.2 (Turbopack)
✓ Compiled successfully in 734ms
  Finished TypeScript in 2.0s
✓ Generating static pages using 6 workers (5/5) in 790ms

Route (app)
┌ ○ /
├ ○ /_not-found
└ ○ /login
```

`npx eslint .` — **نظيف، بلا أخطاء ولا تحذيرات**.

### الاختبار الفعلي

بحساب `n.alharbi@digital-services.test`، والـBackend على `MODEL_PROVIDER=mock`:

| ما جُرِّب | النتيجة |
| -------- | ------- |
| إرسال أول رسالة بلا محادثة نشطة | `POST /api/chat` أعاد `conversation_id`، وظهر الرد، وانضاف صفّ للشريط بعنوان مشتق من الرسالة |
| Enter | يُرسل — الحدث يمنع الافتراضي |
| Shift+Enter | لا يمنع الافتراضي، فيدرج سطرًا جديدًا |
| رسالة من أربعة أسطر | الأسطر محفوظة في الفقاعة، والحقل كبر إلى ١٥٨px ثم توقّف عند السقف ١٦٠px |
| كلمة لاتينية طويلة بلا مسافات | التفّت داخل الفقاعة بلا تمدد أفقي للتخطيط |
| التمرير التلقائي | نزل إلى آخر رسالة بعد كل إرسال |
| اختيار محادثة من الشريط | حُمِّلت رسائلها من `/api/conversations/{id}/messages` وانتقل التمييز |
| إعادة تسمية بعنوان فارغ | «عنوان المحادثة لا يمكن أن يكون فارغًا» ولم يُرسَل طلب `PATCH` |
| إعادة تسمية بعنوان صحيح | انعكست في الشريط فورًا |
| حذف محادثة | تأكيد داخل الواجهة (لا `confirm()`)، ثم اختفى الصفّ فورًا وفُرّغت اللوحة |
| حذف آخر محادثة | ظهرت الحالة الفارغة في الشريط وفي لوحة الشات معًا |
| عرض ٣٧٥px | الشريط انطوى، وزر ☰ فتح درجًا ينزلق من اليمين بخلفية معتمة وزر إغلاق |
| تمرير أفقي على الجوال | لا شيء — `scrollWidth` يساوي عرض النافذة |
| أخطاء JavaScript في الـConsole | لا شيء |

**ما لم يُجرَّب:** الطيّ بلمسة فعلية على جهاز حقيقي (جُرِّب بمحاكاة عرض
٣٧٥px)، وسلوك القائمة مع عدد كبير من المحادثات يستدعي ترقيم الصفحات.

### قرارات وملاحظات للمراجعة

- **الحالة في `Workspace` لا في الشريط ولا في اللوحة.** الاثنان يقرآن القائمة
  ويكتبان فيها: أول رسالة تنشئ صفًّا في الشريط، وحذف صفّ يفرّغ اللوحة.
- **رسالة فشل الإرسال تُزال ويُعاد نصّها إلى الحقل**، فلا يفقد الموظف ما كتبه
  عند انقطاع الشبكة.
- **`isComposing` مستثنى من اختصار Enter:** بدونه تُرسل الرسالة في منتصف
  تأليف الحرف في محرّرات الإدخال.
- **المحادثة النشطة مميّزة بلونٍ وشريطٍ جانبي و`aria-current` معًا** — اللون
  وحده لا يكفي لمن لا يميّز الألوان ولا لقارئ الشاشة.
- **أزرار التسمية والحذف مجاورة لزر الاختيار لا داخله:** زر داخل زر HTML غير
  صالح ولا يُنقر بلوحة المفاتيح. وهي ظاهرة دائمًا على الأجهزة اللمسية حيث
  لا وجود لـ`hover`.
- **ثلاثة تعطيلات موضعية لقاعدة `react-hooks/set-state-in-effect`** في
  `AuthProvider` و `ChatPanel` و `Workspace`، ولكلٍّ منها سبب مكتوب عند
  السطر: جلب أولي ومزامنة مع تغيّر خارجي، لا اشتقاق حالة من حالة.
- **بلا ترقيم صفحات في قائمة المحادثات بعد.** الـBackend يدعمه (`limit` و
  `offset`)، والواجهة تطلب الصفحة الأولى فقط. يُضاف إن ظهرت الحاجة.

---

# P3-03 — رفع الملفات وحالات التحميل والأخطاء والإعدادات

- **الحالة:** Not Started
- **تعتمد على:** P3-02
- **الملفات المتوقعة:** `frontend/components/` · `frontend/lib/api.ts` ·
  `frontend/app/`

## ما تشمله

1. **رفع الملفات:** زر رفع وسحب وإفلات، وعرض اسم الملف وحجمه، ومؤشر تقدم،
   وإمكانية الإزالة قبل الإرسال.
2. تحقق في الواجهة من الامتداد والحجم قبل الإرسال، برسالة عربية واضحة.
3. **حالات التحميل:** مؤشر أثناء انتظار الرد، وتعطيل زر الإرسال، وهيكل
   تحميل (Skeleton) لقائمة المحادثات.
4. **معالجة الأخطاء:** رسالة عربية واضحة لكل حالة (السيرفر متوقف، الجلسة
   منتهية، الاشتراك منتهٍ، المزود غير متاح، خطأ عام) مع زر إعادة محاولة.
5. **صفحة إعدادات رابط السيرفر:** الموظف يضبط عنوان سيرفر جهته، ويُحفظ محليًا،
   ويُستخدم في كل الطلبات، مع زر اختبار اتصال يفحص `/health`.

## شرط الإنجاز

- إيقاف الـBackend يعطي رسالة عربية واضحة لا شاشة بيضاء ولا خطأ في الـConsole.
- تغيير رابط السيرفر ينعكس على كل الطلبات بعد الحفظ.

```
اقرأ docs/PERSON_3.md.
نفّذ المهمة P3-03. تأكد من أنك داخل C:\Users\abdul\govagent-repo عبر
git rev-parse --show-toplevel قبل أي تعديل.
لا تعمل Commit أو Push أو Merge، ولا تنشئ فروعًا أو Issues.

المطلوب:
1. أضف رفع الملفات: زر رفع وسحب وإفلات، وعرض الاسم والحجم، ومؤشر تقدم،
   وإمكانية الإزالة قبل الإرسال.
2. تحقق في الواجهة من الامتداد والحجم قبل الإرسال، برسالة عربية واضحة.
3. أضف حالات التحميل: مؤشر أثناء انتظار الرد، تعطيل زر الإرسال، وSkeleton
   لقائمة المحادثات.
4. أضف رسائل خطأ عربية واضحة لكل حالة: السيرفر متوقف، الجلسة منتهية،
   الاشتراك منتهٍ، مزود المودل غير متاح، وخطأ عام — مع زر إعادة محاولة.
   ممنوع الشاشة البيضاء أو الخطأ الصامت.
5. أضف صفحة إعدادات لرابط سيرفر الجهة: يُحفظ محليًا ويُستخدم في كل الطلبات،
   مع زر اختبار اتصال يفحص /health ويعرض النتيجة.

شغّل: cd frontend && npm run build
في النهاية اذكر الملفات المعدّلة ونتيجة البناء كما ظهرت فعلًا.
```

---

# P3-04 — لوحة مسؤول الجهة

- **الحالة:** Not Started
- **تعتمد على:** P3-01
- **الملفات المتوقعة:** `frontend/app/` · `frontend/components/`

## ما تشمله

1. **لوحة المسؤول:** صفحة محمية بالدور `admin` فقط، والموظف العادي لا يراها
   ولا يصل إليها برابط مباشر.
2. **إدارة الموظفين:** قائمة، وإضافة، وتعطيل، وحذف، وتغيير الدور، مع تأكيد
   داخل الواجهة قبل أي إجراء مدمّر.
3. **عرض الاشتراك والتراخيص:** تاريخ الانتهاء، وعدد المقاعد المستخدمة من
   الإجمالي، وتنبيه واضح عند الاقتراب من الانتهاء أو تجاوز المقاعد.
4. **سجل التدقيق (Audit Logs):** جدول بالأحداث مع الوقت والمستخدم والنوع،
   وفلترة بالتاريخ والنوع، وترقيم الصفحات.
5. حالات فارغة وحالات خطأ لكل قسم.

## شرط الإنجاز

- موظف عادي يفتح رابط اللوحة يُمنع برسالة واضحة.
- الأرقام المعروضة (المقاعد، تاريخ الانتهاء) تأتي من الـAPI لا ثابتة في الكود.

```
اقرأ docs/PERSON_3.md.
نفّذ المهمة P3-04. تأكد من أنك داخل C:\Users\abdul\govagent-repo عبر
git rev-parse --show-toplevel قبل أي تعديل.
لا تعمل Commit أو Push أو Merge، ولا تنشئ فروعًا أو Issues.

المطلوب:
1. أنشئ لوحة مسؤول الجهة كصفحة محمية بالدور admin فقط. الموظف العادي يُمنع
   حتى لو فتح الرابط مباشرة، برسالة واضحة.
2. أضف إدارة الموظفين: قائمة، إضافة، تعطيل، حذف، وتغيير الدور، مع تأكيد داخل
   الواجهة قبل أي إجراء مدمّر.
3. أضف عرض الاشتراك والتراخيص: تاريخ الانتهاء، والمقاعد المستخدمة من
   الإجمالي، وتنبيه عند الاقتراب من الانتهاء أو تجاوز المقاعد.
4. أضف عرض سجل التدقيق: جدول بالوقت والمستخدم ونوع الحدث، مع فلترة بالتاريخ
   والنوع وترقيم صفحات.
5. أضف حالة فارغة وحالة خطأ لكل قسم. لا تضع أي رقم ثابت في الكود — كل
   الأرقام من الـAPI.

شغّل: cd frontend && npm run build
في النهاية اذكر الملفات المعدّلة ونتيجة البناء كما ظهرت فعلًا.
```

---

# P3-05 — ربط الـAPIs و PWA والتجاوب النهائي

- **الحالة:** Not Started
- **تعتمد على:** P3-02 · P3-03 · P3-04 · وعمل PERSON_2
- **الملفات المتوقعة:** `frontend/lib/api.ts` · `frontend/app/` ·
  `frontend/public/`

## ما تشمله

1. **ربط كل الواجهة بالـAPIs الحقيقية** وإزالة البيانات التجريبية المؤقتة من
   المسارات التي صار لها API فعلي.
2. إرسال الـJWT في كل طلب محمي، والتعامل مع انتهاء الجلسة بتحويل إلى الدخول.
3. **PWA:** ملف `manifest` وأيقونات و`Service Worker` بسيط، بحيث يظهر خيار
   «تثبيت التطبيق» ويعمل بعد التثبيت.
4. **التجاوب (Responsive):** جوال ولوحي ومكتبي، والشريط الجانبي يتحول إلى
   قائمة منزلقة على الجوال.
5. **الصقل النهائي:** تباين ألوان مقبول، وتركيز لوحة مفاتيح واضح، وعناوين
   صفحات، وتنظيف تحذيرات الـConsole.

## شرط الإنجاز

- مسار كامل يعمل: دخول ← محادثة ← رسالة ← رد ← تسجيل خروج.
- خيار التثبيت يظهر في Chrome، والواجهة سليمة على عرض ٣٦٠ بكسل.

```
اقرأ docs/PERSON_3.md.
نفّذ المهمة P3-05. تأكد من أنك داخل C:\Users\abdul\govagent-repo عبر
git rev-parse --show-toplevel قبل أي تعديل.
لا تعمل Commit أو Push أو Merge، ولا تنشئ فروعًا أو Issues.

المطلوب:
1. اربط كل الواجهة بالـAPIs الحقيقية، وأزل البيانات التجريبية من كل مسار صار
   له API فعلي. أبقِ ما لا API له، واذكره صراحة في تقريرك.
2. أرسل الـJWT في كل طلب محمي، وعالج انتهاء الجلسة بتحويل المستخدم إلى صفحة
   الدخول برسالة واضحة.
3. فعّل PWA: manifest وأيقونات وService Worker بسيط، بحيث يظهر خيار تثبيت
   التطبيق ويعمل بعد التثبيت.
4. اضبط التجاوب على الجوال واللوحي والمكتبي، واجعل الشريط الجانبي قائمة
   منزلقة على الجوال.
5. اصقل النهائيات: تباين ألوان مقبول، تركيز لوحة مفاتيح واضح، عناوين صفحات،
   وتنظيف تحذيرات الـConsole.

شغّل: cd frontend && npm run build
في النهاية اذكر الملفات المعدّلة ونتيجة البناء كما ظهرت فعلًا، وما جرّبته
يدويًا وما لم تجرّبه.
```

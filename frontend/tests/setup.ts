/**
 * تهيئة بيئة اختبارات الواجهة.
 *
 * jsdom لا ينفّذ `scrollIntoView` — ليس فيه تخطيط ولا تمرير — والمكوّنات
 * تستدعيه بعد كل رسالة. بلا هذا البديل يفشل كل اختبار يرسم لوحة المحادثة
 * لسبب لا علاقة له بما يُختبر.
 */
import { vi } from "vitest";

Element.prototype.scrollIntoView = vi.fn();

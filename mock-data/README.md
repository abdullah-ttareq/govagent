# بيانات تجريبية

بيانات خيالية بالكامل للتطوير والاختبار المحلي. **لا تُستخدم في أي بيئة حقيقية،
ولا تُضاف إليها بيانات حكومية أو شخصية حقيقية.**

## المحتوى الحالي

| الملف                  | ما فيه                                              |
| ---------------------- | --------------------------------------------------- |
| `organizations.json`   | ثلاث جهات بمستخدميها واشتراكاتها — ٨ مستخدمين        |
| `conversations.json`   | سبع محادثات بأطوال مختلفة ورسائل عربية               |
| `files.json`           | بيانات خمسة ملفات، منها واحد بصيغة غير مدعومة        |
| `files/`               | الملفات نفسها — `.txt` و `.docx` و `.pdf` و `.xlsx`  |

- `organizations.json` — ثلاث جهات تجريبية بمستخدميها واشتراكاتها.
  - `digital-services` — اشتراك **فعّال**، ١٠ مقاعد.
  - `urban-planning` — اشتراك **فعّال**، ٥ مقاعد، وفيه حساب معطّل عمدًا.
  - `national-archive` — اشتراك **منتهٍ** عمدًا، لاختبار منع الطلبات (QA-06).

**الجهتان الأوليان معًا** تُستخدمان لاختبار عزل البيانات بـ`organization_id`
(QA-07)، ولذلك يجب أن تبقى **كلتاهما فعّالة**: جهة لا يُسجَّل الدخول إليها لا
تصلح طرفًا في إثبات العزل. ولذلك أُفردت جهة ثالثة لحالة الانتهاء، فتُختبر
الحالتان معًا بلا تعارض.

## كلمات المرور

الملف لا يحتوي كلمات مرور. عند تحميل هذه البيانات في قاعدة البيانات، تُولَّد
كلمة مرور موحّدة للتطوير المحلي وتُخزَّن **مُجزّأة** فقط.

كلمة مرور التطوير المتفق عليها تُحدَّد في مهمة QA-01 وتُوثَّق هنا. هي للتطوير
المحلي فقط ولا تُستخدم في أي نشر حقيقي.

مؤقتًا (منذ P2-01): مخزن الذاكرة `DATA_STORE=memory` يزرع هؤلاء المستخدمين
بكلمة مرور واحدة هي قيمة `DEV_SEED_PASSWORD` في `.env` — افتراضها
`GovAgent@2026`. عند تثبيت كلمة المرور في QA-01 يكفي تغيير هذا المتغير بلا
تعديل كود.

## الاشتراكات في مخزن الذاكرة

**مخزن الذاكرة يزرع الحالات نفسها حرفيًا** — الحالة والمقاعد والتواريخ:

| الجهة               | الحالة   | المقاعد | الانتهاء   |
| ------------------- | -------- | ------- | ---------- |
| `digital-services`  | active   | ١٠      | 2030-01-01 |
| `urban-planning`    | active   | ٥       | 2030-01-01 |
| `national-archive`  | expired  | ٥       | 2025-06-30 |

لا انحراف بين هذا الملف والمخزن. عند تعديل حالة هنا يجب تعديلها في
`backend/app/services/organization_settings_store.py` و
`backend/app/services/user_store.py` — التطابق يدوي لأن صورة الـDocker
تنسخ `backend/app` فقط ولا تصل إلى هذا الملف.

يحرس التطابقَ اختبارٌ في `backend/tests/test_subscriptions.py` يفشل إن
توقفت جهة فعّالة عن كونها كذلك، أو إن تغيّر تاريخ الجهة المنتهية.

## المحادثات — `conversations.json`

سبع محادثات بأطوال **مقصودة الاختلاف**: ثلاث برسالتين، واثنتان بأربع رسائل،
وواحدة بعشر، وواحدة **فارغة بلا رسائل**. إحداها في الجهة المنتهي اشتراكها.

الرسائل بالترتيب الزمني الصاعد — من الأقدم إلى الأحدث — كما يعيدها
`GET /api/conversations/{id}/messages`.

صاحب المحادثة يُذكر بالبريد لا بمعرّف رقمي: المعرّفات تُسنَد وقت التشغيل،
والبريد يربط الملف بـ`organizations.json` ربطًا مستقرًا.

**المحادثة خاصة بصاحبها لا بجهته:** فيها محادثة لموظف ومحادثة لمسؤول جهته،
وكلٌّ لا يرى الأخرى — وهذا ما تثبته محادثات `urban-planning`.

## الملفات — `files.json` و `files/`

| الملف                             | الصيغة  | الجهة | المتوقع        |
| --------------------------------- | ------- | ----- | -------------- |
| `لائحة-الإجازات.txt`              | `.txt`  | ١     | 201 `processed` |
| `دليل-الخدمات-الإلكترونية.docx`   | `.docx` | ١     | 201 `processed` |
| `نموذج-طلب-إجازة.pdf`             | `.pdf`  | ١     | 201 `processed` |
| `تعميم-اعتماد-المخططات.txt`       | `.txt`  | ٢     | 201 `processed` |
| `جدول-الميزانية.xlsx`             | `.xlsx` | ١     | **415** غير مدعومة |

المحتوى **مُختلَق بالكامل** ولا يمثل أي وثيقة حكومية. ملف الجهة الثانية طرفٌ
في اختبار العزل: موظفو الجهة الأولى لا يرونه، وطلبه بمعرّفه يعيد 404.

نصّ ملف الـPDF **إنجليزي عمدًا**: إظهار العربية في PDF يستلزم تضمين خط عربي
كامل وتشكيل الحروف يدويًا، وهو ثقل بلا فائدة للاختبار. الاسم عربي، والمطلوب
أن يُستخرج نصّه ويُفهرَس — وقد تحقّق ذلك بـ`pypdf` نفسها التي يستعملها
الـBackend.

حالتان لا تصلحان ملفات في المستودع وتُولَّدان عند الحاجة: **الحجم الزائد**
(أكبر من ١٠ ميجابايت ← 413) و**الاسم بلا امتداد** (← 415).

## مجموعة Postman

المسارات كلها مغطّاة في
[`docs/postman/`](../docs/postman/README.md) — ٩ مجلدات و٤٢ طلبًا، وتستخدم
حسابات هذا الملف نفسها.

/**
 * إعداد البناء — **المكان الوحيد الذي يُذكر فيه عنوان أي خادم.**
 *
 * ⚠️ **لا يُدخل المستخدم رابطًا ولا عنوان سيرفر ولا مفتاح API في أي شاشة.**
 * كان الإصدار السابق يعرض حقل «رابط السيرفر» في الإعدادات؛ أُزيل تمامًا:
 * GovMind صار منتجًا باشتراك، ومن يشتريه لا يعرف عنوان الـBackend ولا يجب
 * أن يعرفه. الرابط يُثبَّت هنا وقت البناء، ويجب أن يطابق
 * `host_permissions` في `manifest.json` — القيمتان تُغيَّران معًا أو لا
 * تُغيَّر أيٌّ منهما، وإلا رفض المتصفح كل طلب بلا رسالة مفهومة.
 *
 * عنوان Supabase ومفتاحه **ليسا هنا**: الإضافة لا تنادي Supabase إطلاقًا،
 * بل تمرّ بالـBackend الذي يحمل المفتاح على السيرفر. بهذا يبقى عنوان واحد
 * في المنتج كله.
 *
 * للتغيير قبل التوزيع: بدّل `BACKEND_URL` هنا و `host_permissions` هناك.
 */

/** عنوان GovMind Backend. بلا `/` في آخره. */
export const BACKEND_URL = "http://localhost:8000";

/** اسم المنتج كما يظهر في النافذة. */
export const PRODUCT_NAME = "GovMind";

/**
 * مهلة أي طلب بالمللي ثانية.
 *
 * أقصر من مهلة المودل على السيرفر بكثير عمدًا: لا يوجد في هذه الإضافة أي
 * طلب توليد نصّ — كل ما تفعله قراءة حالة وتفعيل جهاز وطلب رابط، وكلها
 * أجزاء من ثانية. مهلة طويلة هنا تعني نافذة معلّقة بلا سبب.
 */
export const REQUEST_TIMEOUT_MS = 20000;

/**
 * منافذ GovMind Runtime على هذا الجهاز، بالترتيب الذي تُجرَّب به.
 *
 * الـRuntime يفضّل ٨٧٦٥، وينتقل إلى ما بعده إن كان مشغولًا — فتجرّبها
 * الإضافة بالترتيب نفسه. **لا يُعرض المنفذ للمستخدم ولا يُطلب منه.**
 */
export const RUNTIME_PORTS = [8765, 8766, 8767, 8768, 8769];

/** مهلة فحص منفذ واحد. قصيرة: منفذ مغلق يُعرف فورًا. */
export const RUNTIME_PROBE_TIMEOUT_MS = 1500;

/**
 * ⚠️ **سقف زمني صارم لاكتشاف الـRuntime كله، لا لكل منفذ.**
 *
 * كان الاكتشاف يجرّب المنافذ **بالتتابع**: خمسة منافذ × ١٫٥ ثانية = سبع
 * ثوانٍ ونصف قبل أن ترسم النافذة أي شيء، وأول تثبيت — وهو الحالة
 * الطبيعية — أبطأها كلها لأن لا منفذ يردّ.
 *
 * صارت المنافذ تُجرَّب **معًا** تحت هذه المهلة الواحدة. وغياب الـRuntime
 * ليس خطأً: هو ما يكون قبل التثبيت دائمًا.
 */
export const RUNTIME_DISCOVERY_TIMEOUT_MS = 1000;

/**
 * كم يُنتظر ظهور الـRuntime بعد تنزيل المثبّت، بالمللي ثانية.
 *
 * ١٥ دقيقة: تشمل وقت فتح المستخدم للملف، وموافقته على نافذة ويندوز،
 * وخطوات المثبّت. أقصر من ذلك يجعل الإضافة تستسلم والمثبّت ما زال يعمل.
 */
export const RUNTIME_WAIT_MS = 15 * 60 * 1000;

/** الفاصل بين محاولات العثور على الـRuntime. */
export const RUNTIME_POLL_MS = 2000;

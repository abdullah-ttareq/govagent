# تبعيات GovMind Runtime — تُجمَّع داخل الملف التنفيذي بـPyInstaller.
# **لا يحتاج جهاز العميل بايثون ولا Node.js مثبَّتين.**

fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0
pydantic>=2.7,<3.0
httpx>=0.27,<1.0

# تبعية غير مباشرة لـhttpx، **مذكورة صراحةً لأنها تُحزَم صراحةً** في
# `govmind-runtime.spec`: ملف شهادات TLS، وغيابه يُسقط كل نداء شبكي.
certifi>=2024.2.2

# البناء والاختبار فقط — لا تُثبَّت على جهاز العميل.
pyinstaller>=6.6,<8.0
pytest>=8.0,<9.0

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-dotenv>=1.0,<2.0

# المصادقة: تجزئة كلمات المرور وإصدار JWT.
# bcrypt مباشرة بلا passlib — passlib غير مصانة وتتعطّل مع bcrypt 4.1+.
bcrypt>=4.1,<6.0
PyJWT>=2.8,<3.0

# رفع الملفات: FastAPI يقرأ multipart/form-data عبرها.
python-multipart>=0.0.9,<1.0

# Oracle Database — الدرايفر بوضع thin لا يحتاج Oracle Client مثبّتًا.
# الاتصال كسول، فوجود الحزمة لا يعني فتح أي اتصال عند الإقلاع.
oracledb>=2.4,<4.0

# OCI Generative AI — اختيارية عمدًا وغير مثبّتة افتراضيًا.
# المشروع يعمل بالكامل بـMODEL_PROVIDER=mock بدونها. ثبّتها على سيرفر الجهة
# فقط عند تفعيل MODEL_PROVIDER=oracle عبر: pip install "oci>=2.126,<3.0"
# oci>=2.126,<3.0

# استخراج النص من الملفات المرفوعة (PDF و DOCX).
# الاستيراد كسول داخل الدوال، فرفع ملف .txt لا يحمّل أيًا منهما.
pypdf>=4.2,<7.0
python-docx>=1.1,<2.0

# عميل HTTP. يستخدمه مزود LM Studio في التشغيل، ويستخدمه TestClient في
# الاختبارات. مطلوب دائمًا، لا في الاختبارات وحدها.
httpx>=0.27,<1.0

# اختبارات
pytest>=8.0,<9.0
